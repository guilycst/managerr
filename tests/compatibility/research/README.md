# Compatibility research fixtures

Task C-00 reserves this directory for source and fixture evidence. The current
result is documentation only. Future fixture files must use synthetic titles,
fake endpoints and redacted responses. They must record upstream product/API
version and never include credentials, cookies, tracker URLs or real inventories.

X-05 owns disposable write fixtures. Read-only adapter fixtures belong to X-01
through X-04. Do not use a fixture here to claim a capability that was not tested
against the matching upstream behavior.
