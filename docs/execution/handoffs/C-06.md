# C-06: generation and CI across every standalone client

## Assignment

- Task ID and title: C-06, extend generation and CI across every client module.
- Owner/agent: /root/x05_implementer. Independent reviewer: /root/x05_reviewer.
- Base commit: 8b97f3fa04ec9d80f89df3c11e667cf4e5df4b28.
- Branch/worktree or shared-checkout ownership: shared checkout on main;
  coordinator owns docs/execution/state.json and integration.
- Owned paths: scripts/generate.sh, scripts/check-guardrails.sh,
  scripts/check-lint.sh and .github/workflows/checks.yml, plus this handoff.
  Client source, contracts, generated output and module manifests were not
  modified.
- Dependencies: C-05, X-15, X-16, X-17 and X-18 were present at dispatch.
- Linked acceptance: A-43 and A-45.

## Contract and work

- The root generation entrypoint now regenerates and compares the internal
  generated OpenAPI output for Sonarr, Radarr, Jellyfin and Seerr in addition
  to the existing root, UI, qBittorrent and NZBGet artifacts. All generation
  runs use GOWORK=off, GOPROXY=off and GOSUMDB=off in the generator commands,
  so a warmed tools module supports deterministic offline checks.
- The aggregate guardrail module list now contains root, ui, tools and all six
  standalone clients: clients/qbittorrent, clients/nzbget, clients/sonarr,
  clients/radarr, clients/jellyfin and clients/seerr. Each module must expose
  discoverable packages and passes test, race test, vet and go mod verify.
- The lint matrix and standalone-client architecture check cover all six
  clients. The guardrail fast path also rejects go.work/go.work.sum files and
  replace directives in every module, preserving independent nested-module
  builds without a permanent local replace.
- The aggregate CI guardrail adds Linux amd64 and arm64 CGO-free builds for
  every module. The GitHub workflow has a Go test/race/vet/verify matrix for
  every module, a generation job that checks the root output and each local
  client checker, an API job, a full lint/architecture job, aggregate
  guardrails and a cross-build matrix for both Linux architectures.
- The workflow's generation step runs the pre-existing qBittorrent checker
  with its pinned version resolution after the tools module is downloaded;
  NZBGet, Sonarr, Radarr, Jellyfin and Seerr local checkers run with
  GOPROXY=off and GOSUMDB=off. The authoritative root generation check is
  offline for all eight generated surfaces. A cold checkout still needs the
  normal CI module-download/bootstrap step before qBittorrent's legacy
  module-local checker can resolve its versioned command.
- No client code, upstream contract, root adapter, generated client, local
  replace or go.work file was added. No credentials, private coordinates,
  live inventories or media data were used.

## Verification

All commands were run from the public repository with synthetic fixtures only.

| Command or scenario | Result / exit status | Evidence path |
| --- | --- | --- |
| GOWORK=off GOPROXY=off GOSUMDB=off ./scripts/generate.sh --check | Passed; bundled API and all root/client generated outputs matched | scripts/generate.sh |
| GOWORK=off bash -euo pipefail generation sequence with root staged generation, qBittorrent checker, and offline NZBGet/Sonarr/Radarr/Jellyfin/Seerr checkers | Passed; every local checker completed; qBittorrent ran with its required cached pinned resolution | scripts/generate.sh and clients/*/check-generation.sh |
| ./scripts/check-guardrails.sh --ci | Passed; generation/API/Vacuum, architecture, full lint, test, race, vet, module verification and Linux cross-builds all passed for root, UI, tools and six clients | scripts/check-guardrails.sh |
| python3 scripts/check-architecture.py | Passed; root import boundaries | scripts/check-architecture.py |
| python3 scripts/check_planning.py | Passed; 53 tasks, 60 acceptance cases and links valid | scripts/check_planning.py |
| actionlint .github/workflows/checks.yml and PyYAML parse | Passed; workflow syntax valid | .github/workflows/checks.yml |
| git diff --cached --check and product pre-commit hook | Passed; staged generation/API/architecture/format/targeted checks passed | .githooks/pre-commit |
| GOWORK=off go test -race and go vet for each matrix module | Passed through aggregate guardrail; no detector races or vet failures | scripts/check-guardrails.sh |
| GOOS=linux GOARCH=amd64/arm64 CGO_ENABLED=0 go build -mod=readonly ./... for each module | Passed for both architectures across all nine modules | scripts/check-guardrails.sh and .github/workflows/checks.yml |

The aggregate guardrail script emits the pinned Vacuum quality result of
100/100 with zero warnings/errors and reports zero golangci-lint issues. The
root generation check and each client checker are side-effect-free in check
mode; generated output is committed and never hand-edited.

## Review and integration

- Product commit: 4ca9df839a7187e63f8058807aa11ef847c50f55,
  message ci: cover every standalone client module.
- Independent review is pending against the exact product commit above.
- Coordinator must record the product and handoff SHAs in state.json and
  retain the separate setup history; this lane did not edit state.json.
- C-06 closes the repository matrix/bootstrap contribution. It does not
  publish nested modules, add root adapters, freeze upstream runtime versions,
  resolve G-01 Arr write safety, or claim live CI/deployment behavior.

## Resume checkpoint

- Current product checkpoint: 4ca9df839a7187e63f8058807aa11ef847c50f55.
- Handoff commit: pending until this file is committed separately.
- Current state: all six standalone clients are included in generation,
  lint, architecture, module verification, race/vet testing and Linux
  cross-build matrices. No local workspace or replacement dependency is
  permitted by the guardrail.
- Active process or agent ownership: no active process; coordinator owns
  state/integration and /root/x05_reviewer owns the independent review.
- Next safe action: commit this handoff separately, record both exact SHAs in
  state.json, and dispatch the independent review against the final product
  tree.
- Outstanding limitations: a cold checkout requires CI's documented module
  download/bootstrap before generators run; qBittorrent's legacy local
  checker resolves its pinned command through the normal module cache. The
  authoritative root generation command remains offline once tools are warm.
