# C-00 handoff: upstream evidence and capability gates

## Assignment

- Task: C-00, Freeze upstream evidence and capability gates
- Coordinator-owned implementation slice after two stalled Luna attempts
- Original task base: `ef4fbc3668e3f021b24c8e6c0e7ecbe0704b5bd0`
- Coordinator authorization checkpoint: `0bac5f9`
- Current coordinator checkpoint before this slice: `e591b41`
- Owned files: `docs/research/compatibility-matrix.md`,
  `tests/compatibility/research/README.md`, this handoff
- Required acceptance contributions: A-16, A-31, A-54, A-55

The first Luna worker exceeded a bounded research window without files or a
handoff. A retry received a tighter source-backed scope and also produced no
files. Both attempts were interrupted and recorded in coordinator state. This
handoff contains the coordinator's bounded completion; it does not claim either
worker produced evidence.

## Work completed

Added a source-backed matrix with candidate API baselines, inspected source
commits, capability gates and explicit blockers. Added a fixture policy that
prevents real credentials and inventories from entering compatibility tests.
The matrix records the Arr preview/command boundary, qBittorrent operation scope,
NZBGet provenance identifiers and processing states, Seerr read pagination and
Jellyfin refresh uncertainty. It keeps native writes disabled until X-05.

## Verification

| Check | Result |
| --- | --- |
| `gh api repos/araihu/goshtoso/releases/latest --jq '{tag_name,published_at,html_url}'` | Passed earlier planning recheck; Goshtoso v0.3.0, 2026-09-11. |
| `git -C /tmp/managerr-radarr-reference rev-parse HEAD` | `0220f0daa9f68ffa40e5f0fe1ce4f909858ceba4`. |
| `git -C /tmp/managerr-sonarr-reference rev-parse HEAD` | `9c0d8deb01a51b35b85b8dbe296bbff87feb0ffb`. |
| `git -C /tmp/managerr-seerr-reference rev-parse HEAD` | `68c5bc8c7d8560d295387adeeee73982ea518e8f`. |
| Source inspection of Radarr/Sonarr manual-import controllers/services | Passed; details linked in matrix. |
| Public qBittorrent/NZBGet/Seerr documentation recheck | Passed; links and limits recorded in matrix. |
| Live upstream writes or media operations | Not run by design. |

## Remaining gate

X-05 must exercise disposable supported upstreams before X-06/X-07/X-09 can
enable mutation capabilities. G-01 remains open because the native Arr command
can reconstruct import decisions after preview and no atomic cross-actor
no-overwrite guarantee has been demonstrated. This task records the blocker;
it does not weaken I-07.

## Reviewer request

Review the matrix for source accuracy, acceptance coverage, explicit unknowns,
and the no-overwrite blocker. Reviewer must be independent of the coordinator
and the two interrupted implementation attempts. Record decision and any
correction in the reviewer handoff before C-01 opens.

## Review round 1 correction

Reviewer commit `057b1aa347a2b990bda91d759b18c0506bb69783` requested three P1
corrections. The coordinator applied them after both Luna implementation
attempts stalled without a handoff:

- Matrix rows now use `CAP-*` IDs. Authoritative `G-01` through `G-06` meanings
  remain owned by decisions/state.
- Every capability row records product/version candidate, API/schema identity,
  immutable evidence, fixture reference and `PASS`/`FAIL`/`UNKNOWN`. Unknown
  release/fixture choices explicitly block enablement. Seerr request pagination
  has an immutable source link.
- NZBGet `ID` is documented as a deprecated alias of `NZBID`. The fixture varies
  Arr's `drone` value, history retention and FinalDir/DestDir, without inventing
  an independent history identifier. Acceptance A-05 and connector prose match.

Correction changes need a second independent review before C-01 opens. Native
writes remain disabled until the matrix and X-05 evidence clear their gates.

## Review round 2 correction

Round 2 found one evidence blocker: matrix fixtures were placeholders, Result
cells mixed `PASS` and `UNKNOWN`, and NZBGet/qBittorrent references were not
fully immutable. The coordinator corrected the matrix and source ledger:

- Every capability row now has a planned owner and exact fixture path. Planned
  fixtures are explicitly absent, so each row has one normalized `UNKNOWN`
  result and blocked enablement until its owner records a versioned fixture.
- Source observations moved to a separate column. They no longer look like
  capability passes.
- qBittorrent points to page commit `cc6579ab58f534d03b40ae0deba5755c4a342319`,
  blob `82f67b6f5aa73ef54d4c88dcd38c695ae1191b72`, within repository snapshot
  `485b0ee5e6f7e037f301500c25b23833569a2266`.
- NZBGet history and post-processing use source commit
  `b609226e18da11955ce8dda2c7df959258655579` and exact blob identities
  `1807fc83be9af8354801f0ddcb9d7f04da4b7219` and
  `11df5abd79671266308a7eaf6da8d9ccb6f81a43`. The Radarr NZBGet adapter uses
  commit `0220f0daa9f68ffa40e5f0fe1ce4f909858ceba4` and blob
  `fc4cd2ac3872a6593baae50f0aea5a618d4f7d38`.

Round 3 independent review must verify these paths, hashes, normalized results,
and the unchanged no-overwrite blocker before C-01 opens.
