# X-01 handoff

## Assignment

- Task ID and title: X-01, Implement qBittorrent inventory adapter.
- Owner and independent reviewer: `/root/f01_implementer`; reviewer `/root/f01_reviewer`.
- Base commit: `bc75aacbfda920272618d8e76118b4bc5f10df23` (dispatch record).
- Branch/worktree and ownership: shared checkout on `main`; coordinator owns integration and execution state.
- Owned paths: `internal/adapters/qbittorrent/inventory/` and `tests/fixtures/qbittorrent/`.
- Dependency verified: C-03 approved receipt `8c9bf9a6277e068f9ae6b859fab2c5f7aa4890a1`.
- Acceptance contributions: A-04, A-09 and A-28. Planned checks were focused tests, race, vet, full root tests, generation/planning/module checks and relevant cross-compiles.

## Contract and work

- The adapter implements the frozen read-only `ports.DownloadInventoryPort` and `ports.CapabilityPort` contracts. It also exposes `ListDetailed`, `Version`, `ConnectionID` and `ScopedIdentity` for callers that need qBittorrent-specific evidence without widening the common port.
- Session handling uses `/api/v2/auth/login`, a private cookie jar, context-bound requests, bounded response reads, same-origin redirect enforcement and sanitized typed `domain.UpstreamError` values. Credentials, URLs and upstream bodies are never copied into errors or observations.
- Inventory reads qBittorrent WebUI version/application version, torrent summaries, properties, file metadata and optional descriptor export. It preserves state, progress, completion/addition timestamps, seeding, ratio, seed/leech counts, category/tags hints, content/save paths and v1/v2 hash values. Item identity is scoped as `connection-id:external-hash`; the common `ExternalID` remains the upstream hash.
- Pagination uses an instance-authenticated opaque cursor with a source ID, start time, fixed page size, offset, cumulative observed count, all previously observed identities, page fingerprints and a bounded prior-partial marker. Cursor payload and signature are capped at 128 KiB; page and item bounds are capped at 1,000 so every accepted cursor round-trips. The per-instance key makes cursors invalid after restart or across clients. Repeated/overlapping pages, response overflow, offset overflow and upstream caps terminate safely as partial coverage rather than looping or claiming absence. Multi-page scans perform a bounded summary revalidation pass before claiming complete coverage and remain partial when insertion, deletion, duplicate membership, final-page changes or revalidation failure are observed.
- File observations preserve qBittorrent index/name/path/size/progress/priority/availability/seeds/is-seed. `availability=-1` and `seeds=-1` remain visible as unknown sentinels with partial reason evidence. Configured component-aware mappings produce root-relative `FileManifestEntry` values; unmapped or ambiguous files stay visible in detailed observations and cannot silently select a destination.
- Descriptor export is disabled by default. Best-effort mode returns only availability, size, SHA-256 digest, capture time and source after bounded torrent-metainfo validation requiring one non-empty top-level `info` dictionary; descriptor bytes are never returned in an ordinary inventory page. A magnet-only or metadata-missing item reports a distinct unavailable state.
- Categories and tags are read-only provenance hints. No qBittorrent mutation, recategorization, deletion, torrent stop, file move or rename is present in this package.

## Changed commits

- Product implementation and synthetic fixtures: `d74a735b18a2cc5768dcb9f3ff0656f8540d3cff`, `feat(qbittorrent): add read-only inventory adapter`.
- Correction round two product: `dd7156396c649c00190ddc7f25d2e0e12868b1c5`, `fix(qbittorrent): harden inventory reconciliation`.
- Handoff documentation commit: pending; this file is the only remaining owned path to commit.
- No root module files, `state.json`, API contracts, UI files, migrations or other agents' paths were staged.

## Verification

| Command or scenario | Result | Evidence |
| --- | --- | --- |
| `GOWORK=off go test -timeout=120s ./internal/adapters/qbittorrent/inventory` | Passed, exit 0 | focused suite over synthetic WebUI fixtures, including correction regressions |
| `GOWORK=off go test -race -count=1 -timeout=120s ./internal/adapters/qbittorrent/inventory` | Passed, exit 0 | private cookie jars, mutable pagination, cursor authentication, state and descriptor boundaries under race detector |
| `GOWORK=off go vet ./internal/adapters/qbittorrent/inventory` | Passed, exit 0 | adapter package |
| `GOWORK=off go test -cover ./internal/adapters/qbittorrent/inventory` | Passed, 76.5% statement coverage, exit 0 | correction product commit |
| `GOWORK=off go test ./...` | Passed, exit 0 | root packages, storage, filesystem, migrations and qBittorrent adapter |
| `GOWORK=off go test -race -count=1 ./...` | Passed, exit 0 | full root race suite |
| `GOWORK=off go vet ./...` | Passed, exit 0 | full root vet suite |
| `GOWORK=off ./scripts/generate.sh --check` | Passed, exit 0 | generated artifacts current |
| `python3 scripts/check_planning.py` | Passed, exit 0 | 38 tasks and 60 acceptance cases validated |
| `GOWORK=off go mod verify` | Passed, exit 0 | all modules verified |
| `(cd tools && GOWORK=off go test ./... && GOWORK=off go vet ./... && GOWORK=off go mod tidy -diff)` | Passed, exit 0 | tools module checks |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go test -c -o /tmp/mastarr-x01-linux-amd64.test ./internal/adapters/qbittorrent/inventory` | Passed, exit 0 | Linux amd64 compile check |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go test -c -o /tmp/mastarr-x01-linux-arm64.test ./internal/adapters/qbittorrent/inventory` | Passed, exit 0 | Linux arm64 compile check |
| `GOWORK=off CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go test -c -o /tmp/mastarr-x01-windows-amd64.test ./internal/adapters/qbittorrent/inventory` | Passed, exit 0 | Windows amd64 compile check |
| `git diff --cached --check` | Passed before product commit | only owned product and fixture paths staged |
| `TestListDetailedPagesAndMetadata` | Passed | multi-page film/series/pack; full properties/files, subtitle mapping, v2 hash, completion and seeding |
| `TestConnectionScopedIdentityAcrossInstances` | Passed | same upstream hash yields distinct per-connection identities |
| `TestFullPageCursorRoundTrip` | Passed | 200-item page cursor remains bounded and decodes on continuation |
| `TestPaginationBoundsAndObservedCount` | Passed | response overflow and max-item cap produce bounded partial coverage without count overshoot |
| `TestRepeatedPageTerminatesAsPartial` | Passed | repeated upstream page emits no duplicate items and terminates |
| `TestNonAdjacentOverlapAndMutableSnapshotStayPartial`, `TestDeletionBeforeOffsetAndFinalPageChangeStayPartial` | Passed | all-page identity tracking and bounded revalidation detect overlap, insertion, deletion and final-page changes |
| `TestAuthenticatedSessionCookieIsReused` | Passed | login cookie is required and reused without a second login |
| `TestPrivateCookieJarsIsolateConcurrentInstances` | Passed | populated caller jar is not inherited; concurrent clients keep independent SID sessions |
| `TestCursorAuthenticationAndLargestAcceptedRoundTrip` | Passed | HMAC tampering, cross-instance use and the 1,000-item/128 KiB cursor ceiling are rejected or round-tripped |
| `TestTypedErrorsAreSanitized` | Passed | unauthorized, rate-limit, malformed and context-timeout outcomes are typed and redacted |
| `TestMalformedFilesAndVersionRemainVisibleAsPartial` | Passed | malformed optional evidence keeps the torrent visible with partial reason codes |
| `TestProcessingStateAndAvailabilitySentinel` | Passed | processing state and qBittorrent unknown availability remain distinct |
| `TestUnknownTorrentStateCannotAuthorizeProcessing` | Passed | future state remains unknown/partial and cannot set `ProcessingDone`; recognized terminal state can |
| `TestBestEffortDescriptorMetadata` and `TestMalformedTorrentDescriptorRejected` | Passed | bounded export metadata only; malformed bencode unavailable |
| `TestPathMappingAndRemotePathBoundaries` | Passed | traversal rejection, longest component-aware mapping, ambiguity and retained exact file observation |

All fixtures use synthetic hashes, paths and client responses. No live qBittorrent instance, credential, tracker coordinate, real inventory or mutating request was used.

## Review and integration

- Round-one reviewer receipt: `b89ffa1b9fc829134fed84cf55fd88e614243d91`; all six findings were addressed in correction product commit `dd7156396c649c00190ddc7f25d2e0e12868b1c5`.
- Reviewer: pending `/root/f01_reviewer` independent review of correction commit `dd7156396c649c00190ddc7f25d2e0e12868b1c5`.
- Known limits for review: qBittorrent's version endpoints and optional export remain capability evidence, not a product-version pin; file physical digest/identity is unavailable from the inventory API and remains empty; payload mapping requires an explicit configured path mapping; descriptor export is best effort and does not claim availability after metadata/history removal.
- Review focus: authentication/session isolation, typed error redaction, cursor HMAC/bounds/restart semantics, all-page identity and revalidation coverage, v1/v2 identity scoping, path mapping safety, descriptor size/format validation and preservation of partial/unknown evidence.
- Integrated commit and final reviewer decision: pending coordinator review and state update.

## Resume checkpoint

- Product is committed at `dd7156396c649c00190ddc7f25d2e0e12868b1c5`; no active product edits remain. The correction uses deterministic `httptest` fixtures and adds no testcontainers dependency.
- The coordinator must commit this handoff, record the product and handoff SHAs in `docs/execution/state.json`, obtain the independent receipt, and integrate only after review clears findings.
- No conflicting edits were reset, cleaned, rebased or removed. Other agent work remains outside the owned paths.

## Correction round 3

This section records the bounded descriptor identity correction requested by
independent receipt `docs/execution/handoffs/X-01-review-round2.md`, commit
`4dcdc0081011e70234f0b39f7c7ce5c3758df07d`.

- Correction base: `ab5cdff7c0c82041247e606047a4ce20f43caac3`.
- Product implementation: `f2f4de0dbc5018de51064f78df546746fb226a1a`,
  `fix(qbittorrent): bind exported descriptors to torrent hashes`.
- Handoff documentation commit: pending this documentation commit.
- Owned paths changed: `internal/adapters/qbittorrent/inventory/`,
  `tests/fixtures/qbittorrent/`, and this handoff. No contracts, state,
  module files, D-01 paths, or other lane files were staged.

The export parser now retains the exact raw bencoded `info` value span, then
computes its SHA-1 and SHA-256 identities. Descriptor availability requires a
match against every valid supported value from qBittorrent `hash`,
`infohash_v1`, and `infohash_v2`; a structurally valid descriptor belonging to
another torrent remains unavailable with the stable sanitized reason
`export_identity_mismatch` and contributes partial coverage. Descriptor bytes
remain absent from ordinary inventory responses. Existing bounded and
malformed-response behavior remains unchanged.

Synthetic fixtures cover matching v1, matching v2, a hybrid record with both
supported identities (and a different primary `hash`, proving alternate hash
matching), and a structurally valid wrong-hash export. The raw-span assertion
and prior malformed/trailing-data checks remain in
`internal/adapters/qbittorrent/inventory/inventory_test.go`.

The following checks passed for the product correction (all exit 0):

| Command or scenario | Result |
| --- | --- |
| `GOWORK=off go test -count=1 ./internal/adapters/qbittorrent/inventory` | Passed focused suite. |
| `GOWORK=off go test -race -count=1 -timeout=180s ./internal/adapters/qbittorrent/inventory` | Passed focused race suite. |
| `GOWORK=off go vet ./internal/adapters/qbittorrent/inventory` | Passed. |
| `GOWORK=off go test -count=1 -cover ./internal/adapters/qbittorrent/inventory` | Passed, 77.4% statement coverage. |
| `GOWORK=off go test -count=1 ./internal/adapters/qbittorrent/inventory ./internal/domain ./internal/ports ./internal/bootstrap ./internal/filesystem/observe ./internal/storage ./migrations` | Passed tracked root package slice. |
| `./scripts/generate.sh --check` | Passed; generation checks clean. |
| `GOWORK=off go mod verify` | Passed. |
| `python3 scripts/check_planning.py` | Passed: 38 tasks, 60 acceptance cases, local links resolve. |
| `(cd tools && GOWORK=off go test ./... && GOWORK=off go vet ./... && GOWORK=off go mod tidy -diff)` | Passed. |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go test -c -o /tmp/mastarr-x01-linux-amd64.test ./internal/adapters/qbittorrent/inventory` | Passed. |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go test -c -o /tmp/mastarr-x01-linux-arm64.test ./internal/adapters/qbittorrent/inventory` | Passed. |
| `GOWORK=off CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go test -c -o /tmp/mastarr-x01-windows-amd64.test.exe ./internal/adapters/qbittorrent/inventory` | Passed. |
| `git diff --check` and `git diff --cached --check` | Passed for product and handoff changes. |

`GOWORK=off go test -count=1 ./...` exited 0 during this correction before an
untracked ARR lane appeared in the shared checkout. A later full race attempt
was not a clean X-01 result: it observed another lane's in-progress untracked
`internal/adapters/arr/read` package missing required files and untracked
NZBGet tests failing unrelated expectations. The focused X-01 race suite and
tracked root package slice passed. Coordinator should rerun repository-wide
gates after those lanes settle.

Acceptance contribution: A-04 now binds best-effort qBittorrent descriptor
availability to requested v1/v2 identity evidence; A-09 and A-28 retain the
previously cleared connection-scoped inventory, state, pagination, mapping and
partial-coverage behavior. No live qBittorrent instance, credentials, tracker
coordinates, media payload, upstream write, filesystem mutation, release or
deployment was used.

## Resume checkpoint

- Current state: product correction is committed at
  `f2f4de0dbc5018de51064f78df546746fb226a1a`; handoff documentation remains
  to be committed separately.
- Active shared-checkout ownership: unrelated untracked ARR and NZBGet lane
  files are present; they were preserved and not staged.
- Next safe action: `/root/f01_reviewer` independently reviews exact product
  commit, focusing on raw bencoded info-span hashing, all supported hash
  variants, mismatch partial state, and retained descriptor bounds.
- Blocker: independent review receipt and coordinator state/integration remain
  pending. Repository-wide race gate needs a settled shared checkout.
- No conflicting writes or unknown files removed: confirmed; only X-01
  product paths and this handoff are owned here.
