# X-05 handoff: upstream write safety evidence

## Assignment

- Task ID and title: X-05, prove upstream write safety in disposable fixtures.
- Owner/agent and independent reviewer: `/root/x05_implementer`; reviewer
  `/root/x05_reviewer`.
- Dispatch base: `cf987d20e54e76c302a47118369c65e5e13d758a`.
- Branch/worktree ownership: shared checkout on `main`; coordinator owns
  integration and `docs/execution/state.json`.
- Owned product paths: `tests/compatibility/writes/` and
  `docs/research/write-capabilities.md`.
- Owned evidence path: this handoff only. No state file was changed.
- Dependencies verified from the dispatch record: C-00
  `ad4a7dc491c8d66d079880b13a0094a4b58a65ed`, X-12
  `37f62b515681f7ad37fc9b1d4597581f069320e5`, X-13
  `f54ec97c70d75a58099b92e7ee8350220599d009`, X-03
  `31243c71d90717ffa8ca7404817fca541b8ba935`, X-04
  `b5594b668185245304f70a0f46c1f77ae7b76d7b`, and F-01
  `c39d906bd1544aa41de6821c46ce760571c9e374`.
- Acceptance contributions: A-16, A-17, A-28, A-29, A-31 and A-55.

## Contract and work

- Linked requirements: X-05 in `docs/plans/implementation.md`; Arr, qBittorrent,
  Jellyfin and explicit mapping rules in
  `docs/specs/spec-001-media-reconciliation/connectors.md`; A-16/A-17/A-28/
  A-29/A-31/A-55 in `docs/verification/acceptance.md`; G-01 in
  `docs/architecture/decisions.md`.
- Product commit: `597e793fa37c4707f066380ecc91b19be5bf19ef`
  (`test(compatibility): prove upstream write safety boundaries`). It adds five
  synthetic JSON fixtures and a standard-library `httptest` suite.
- Arr probes cover native preview rejection bypass, an externally synchronized
  destination replacement between preview and `ManualImport` command, and a
  two-file pack where command completion yields one observed file. Reconcile
  results are per-file and preserve unresolved items.
- qBittorrent probes cover `stalledUP` with zero upload speed, lost stop
  response followed by read-back and external resume, whole-torrent stop plus
  `/api/v2/torrents/delete` with `deleteFiles=false`, whole-folder rename,
  selected-file scope refusal, exact file read-back and rename collision.
- Jellyfin probe records `202 Accepted` for library refresh separately from
  absent/later-visible item observations, rejects wrong library-root mapping,
  and leaves item refresh unsupported.
- Mapping probe preserves explicit anime episode IDs/absolute numbers, forced,
  SDH and language labels, IDX/SUB pairing, and unresolved unmatched
  companions; duplicate episode identities are rejected.
- `docs/research/write-capabilities.md` records synthetic results, route
  semantics, frozen downstream safety rules and the unresolved G-01 blocker.
  No native Arr write capability is enabled from this evidence. qBittorrent and
  Jellyfin runtime version gates remain unverified where the matrix lacks a
  pinned product build/endpoint.
- No shared contract change requested. X-06 must not enable Arr import while
  G-01 remains open. X-07/X-09 consume the safety rules after their own
  implementation and review.

## Verification

| Command or scenario | Commit / fixture version | Result / exit status | Evidence |
| --- | --- | --- | --- |
| `GOWORK=off go test -mod=readonly -count=1 ./tests/compatibility/writes` | `597e793`; five JSON fixtures | Passed, exit 0 | Arr/qBittorrent/Jellyfin/mapping probes in `tests/compatibility/writes/writes_test.go` |
| `GOWORK=off go test -race -mod=readonly -count=1 ./tests/compatibility/writes` | `597e793` | Passed, exit 0 | Deterministic barrier and shared fixture state checks |
| `GOWORK=off go vet -mod=readonly ./tests/compatibility/writes` | `597e793` | Passed, exit 0 | Focused package vet |
| `GOWORK=off go test -mod=readonly -count=1 ./...` | `597e793` plus current coordinator D-02 commits | Passed, exit 0 | Root packages and compatibility probes |
| `GOWORK=off go vet -mod=readonly ./...` | same checkout | Passed, exit 0 | Root vet |
| Root, UI, tools, qBittorrent and NZBGet `GOWORK=off go test -mod=readonly -count=1 ./...` | same checkout | Passed, exit 0 | Independent module matrix |
| Root, UI, tools, qBittorrent and NZBGet `GOWORK=off go vet -mod=readonly ./...` | same checkout | Passed, exit 0 | Independent module matrix |
| Root, UI, tools, qBittorrent and NZBGet `GOWORK=off go mod verify` | same checkout | Passed, `all modules verified` | Independent module matrix |
| `./scripts/generate.sh --check` | same checkout | Passed | Generated artifacts unchanged |
| `./scripts/check-api.sh` | same checkout | Passed; Vacuum quality 100/100 | Bundled API and Vacuum |
| `python3 scripts/check-architecture.py` | same checkout | Passed | Import boundaries |
| `./scripts/check-lint.sh` | same checkout | Passed; 0 issues | Root/UI/client lint and architecture |
| `python3 scripts/check_planning.py` | same checkout | Passed; 43 tasks, 60 acceptance cases | Planning links |
| `./scripts/check-guardrails.sh --fast` | same checkout | Passed | Generation/API/architecture/targeted Go/format guardrails |
| `git diff --check` | product commit and handoff worktree | Passed | No whitespace errors |
| Native upstream services, credentials, real files, live media or live mutations | intentionally excluded | Not run by design | Local `httptest` handlers and synthetic bytes only |

## Review and integration

- Reviewer identity/role and reviewed commit: pending independent review by
  `/root/x05_reviewer` of `597e793fa37c4707f066380ecc91b19be5bf19ef`.
- Review path: `docs/execution/handoffs/X-05-review-round1.md`.
- Findings, fixes, final reviewer decision and integrated SHA: pending;
  coordinator records them in `docs/execution/state.json`.
- Handoff commit: pending; this file is the only intended handoff change.

## Resume checkpoint

- Product state: synthetic write evidence is committed at
  `597e793fa37c4707f066380ecc91b19be5bf19ef`; no owned product edits remain.
- G-01 remains open. The Arr race probe shows native command execution can
  replace a destination changed after preview, with no tested atomic
  compare-and-swap boundary. Native Arr import stays blocked pending later
  coordination evidence or an explicit product decision for weaker behavior.
- qBittorrent stop/remove/rename conclusions are protocol-level synthetic
  evidence; the compatibility matrix still lacks a pinned product build for
  runtime enablement. Jellyfin refresh has the same versioned-endpoint gap.
- Next safe action: independent reviewer checks the exact product commit; then
  coordinator records review/integration SHAs and reruns repository-wide gates.
- No conflicting writes or unknown files were removed. No state, contract,
  module, adapter or workflow paths were changed.
