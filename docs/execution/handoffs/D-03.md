# D-03 configuration handoff

## Assignment

- Task ID and title: D-03, implement effective configuration and source ownership.
- Owner/agent and independent reviewer: `/root/d02_implementer`; independent review pending coordinator assignment.
- Base commit: `f53cdd2` (D-02 and the prior execution checkpoint).
- Branch/worktree or shared-checkout ownership, repository-relative public notation: shared `main` checkout; the coordinator owns `docs/execution/state.json` and integration.
- Owned files and generated outputs: `internal/configuration/`; no generated output. This handoff is the only additional path in the D-03 lane.
- Dependencies verified at commits: D-02 credential envelope/key package is available in the execution baseline; root storage/schema and API contracts remain coordinator-integrated dependencies.
- Required acceptance IDs and exact planned commands: A-38, A-39 and A-42; focused configuration tests with race/vet, root tests/vet/module verification, API/generation, architecture, lint, planning, guardrails and Linux compile checks.

## Contract and work

- Linked specification sections and relevant invariants: `docs/specs/spec-001-media-reconciliation/configuration.md` resource ownership, startup-only YAML, credential references and key metadata; `docs/specs/spec-001-media-reconciliation/data-and-recovery.md` configuration startup and encrypted credential boundaries; A-38/A-39/A-42 in `docs/verification/acceptance.md`.
- Intended result and capability limits: provide a source-aware effective configuration boundary. YAML is strict, read-only and resolved once per activation; API resources are revision-checked and editable; both sources are merged only after complete validation. Static credentials remain in the in-memory startup snapshot, managed values pass through `credentials.Manager` and an encrypted-envelope store, and no plaintext is exposed by reads or metadata.
- Changes completed / remaining:
  - Added strict OpenAPI-independent YAML loading with bounded regular-file reads, one-document/known-field validation, source document/revision metadata, policy parsing, credential env/file reference validation and startup resolution.
  - Added source ownership and API/YAML collision checks for connections, storage roots and path mappings. YAML resources are read-only through API operations; removed YAML connections and roots become historical retired records, while mapping history is retained within the current domain contract.
  - Added immutable effective snapshots, deterministic source revisions, root-relative path validation, component-aware mapping prefixes, most-specific mapping ambiguity rejection and complete foreign-reference validation before activation.
  - Added explicit `ApplyYAML`/`ApplyYAMLFile` seams without a watcher or automatic reload. YAML activation validates the merged API/YAML candidate before invalidation or map replacement, and authority-bearing YAML changes are reported deterministically to the invalidator.
  - Added API create/patch/retire operations with If-Match-style revisions, source read-only errors, deleted-ID reuse protection, mapping dependency protection and authority-change invalidation. Secret-only managed credential replacement advances the revision but does not invalidate plans.
  - Added encrypted managed credential replacement with complete-set atomic store semantics, redacted credential metadata, static/managed resolution and zeroing of package-owned secret buffers. Nested capability evidence is deep-cloned for snapshot safety.
  - Downstream storage/sqlc translation, HTTP handlers, readiness/key-check persistence and final end-to-end acceptance evidence remain outside this lane.
- Shared contract changes requested from coordinator: add a direct pinned `gopkg.in/yaml.v3` requirement to the root `go.mod`/`go.sum`. The package imports the already selected transitive version `v3.0.1`, but `GOWORK=off` root checks correctly reject the current manifest without a direct requirement. Coordinator owns module files.
- Changed paths and tested commit: product `3e86f11` (`feat(configuration): add effective source-owned configuration`).

## Verification

| Command or scenario | Commit / fixture version | Result / exit status | Evidence path |
| --- | --- | --- | --- |
| `gofmt -w internal/configuration/*.go` and `git diff --check` | `3e86f11`; synthetic source fixtures | Passed | `internal/configuration/*.go` |
| Temporary modfile with `gopkg.in/yaml.v3@v3.0.1`: `GOWORK=off go test -modfile=... -count=1 ./internal/configuration` | `3e86f11` | Passed, exit 0 | `internal/configuration/configuration_test.go` |
| Temporary modfile with `gopkg.in/yaml.v3@v3.0.1`: `GOWORK=off go test -modfile=... -race -count=1 ./internal/configuration` | `3e86f11` | Passed, exit 0 | configuration snapshot/concurrent-read tests |
| Temporary modfile with `gopkg.in/yaml.v3@v3.0.1`: `GOWORK=off go vet -modfile=... ./internal/configuration` | `3e86f11` | Passed, exit 0 | configuration package |
| Temporary modfile with `gopkg.in/yaml.v3@v3.0.1`: `GOWORK=off go test -modfile=... ./...` | `3e86f11` plus concurrent X-07 product | Passed, exit 0 | root packages |
| `GOWORK=off go mod verify` | `3e86f11` | Passed, `all modules verified` | root module cache |
| `cd ui && GOWORK=off go test -mod=readonly ./...` | `3e86f11` | Passed, exit 0 | UI module |
| `./scripts/check-api.sh` | `3e86f11` | Passed; generation and Vacuum quality 100/100 with zero warnings/errors | bundled `api/openapi.yaml` and generated output |
| `python3 scripts/check-architecture.py` | `3e86f11` | Passed | root import boundary check |
| `./scripts/check-lint.sh --architecture-only` | `3e86f11` | Passed | root/client architecture checks |
| `python3 scripts/check_planning.py` | `3e86f11` | Passed; 43 tasks, 60 acceptance cases; local links resolve | planning documents |
| `./scripts/check-guardrails.sh --fast` | `3e86f11` | Passed | generation/API/architecture/format/targeted guardrails |
| `GOWORK=off go test -mod=readonly ./...` | `3e86f11` | Blocked before package tests: direct `gopkg.in/yaml.v3` requirement is missing; no root manifest changed by this lane | coordinator module follow-up |
| `./scripts/check-lint.sh` and `./scripts/check-guardrails.sh --ci` | `3e86f11` | Blocked at root module discovery for the same missing direct YAML requirement; no lint result claimed | coordinator module follow-up |
| Temporary modfile Linux compile: `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 GOWORK=off go test -modfile=... -run '^$' -c ./internal/configuration`; same for `arm64` | `3e86f11` | Passed, exit 0 for both targets | `/tmp/mastarr-configuration-linux-amd64.test`, `/tmp/mastarr-configuration-linux-arm64.test` |
| A-38: API edit/delete of YAML, duplicate IDs/collisions, removed startup resources | `3e86f11`; synthetic YAML | Passed for the owned configuration boundary: read-only/collision errors, full-candidate rejection and retained historical connection/root records | `configuration_test.go` |
| A-39: YAML and secret-file changes without explicit apply; stale managed ETag | `3e86f11`; temporary directories and synthetic secrets | Passed for the owned boundary: cached static value/snapshot unchanged until explicit apply, restart/apply is atomic, stale revision rejected | `configuration_test.go` |
| A-42: static references, managed encrypted save/read, redacted metadata and arbitrary API file reference | `3e86f11`; synthetic values and `credentials.Manager` | Passed for the owned boundary: YAML refs resolve once, API references are rejected, ciphertext is stored, resolution returns copies and metadata is redacted | `configuration_test.go`, `internal/credentials/credentials_test.go` |
| Live media services, real credentials, private coordinates, or upstream/filesystem mutations | `3e86f11` | Intentionally not run | synthetic fixtures only |

The product commit was created while unrelated X-07 files were present in the
shared checkout; only `internal/configuration/` was staged. No state file,
module manifest, script, generated output or unrelated adapter path was changed
by this lane.

## Review and integration

- Reviewer identity/role and reviewed commit: pending independent reviewer assignment.
- Findings with severity, reproduction and contract reference: pending review.
- Fix commit and regression evidence: no reviewer fixes pending at handoff.
- Final reviewer decision: pending.
- Integrated commit, recorded by coordinator: pending; coordinator owns `docs/execution/state.json`.

## Resume checkpoint

- Current state and outstanding uncertainty: D-03 product is committed at `3e86f11`. The package is ready for independent review and downstream adapter wiring. Path-mapping retirement has no `RetiredAt` field in the current `domain.PathMapping`, so its historical record is retained but cannot expose a retirement timestamp until the shared domain contract changes.
- Active process or agent ownership, sanitized: shared `main` checkout; coordinator/X-07 work may advance `HEAD` independently. D-03 owns no active uncommitted paths after this handoff commit.
- Next safe action: coordinator adds the direct pinned YAML dependency, reruns root CI-equivalent checks, dispatches independent review, then integrates configuration with storage/API translation and readiness/key-check persistence.
- Blocker and exact input/evidence needed: direct `gopkg.in/yaml.v3` requirement in the root module; downstream persistent store adapter implementing `ManagedCredentialStore`; reviewer decision for `3e86f11`.
- No conflicting writes or unknown files removed: confirmed; state, scripts, modules, generated outputs, adapters and UI paths were left to their owners.
