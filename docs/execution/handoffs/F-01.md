# F-01 handoff

## Assignment

- Task ID and title: F-01, Implement root-constrained filesystem observations.
- Owner/agent and independent reviewer: `/root/f01_implementer`; round-one receipt `e8c58feb59418b7237759671a039150039e1a79b`; round-two review pending `/root/f01_reviewer`.
- Base commit: `74d4ebe0727686bbfe0c156721bb40cd940b5195`.
- Branch/worktree or shared-checkout ownership, repository-relative public notation: shared checkout; coordinator owns integration and execution state.
- Owned files and generated outputs: `internal/filesystem/observe/`.
- Dependencies verified at commits: C-03 approved receipt `8c9bf9a6277e068f9ae6b859fab2c5f7aa4890a1`.
- Required acceptance IDs and exact planned commands: A-19, A-21, A-23; `GOWORK=off go test ./internal/filesystem/observe -count=1`, `GOWORK=off go test -race ./internal/filesystem/observe -count=1`, `GOWORK=off go vet ./internal/filesystem/observe`, Linux amd64/arm64 and Windows compile checks, `git diff --check`.

## Contract and work

- Linked specification sections and relevant invariants: `spec-001-media-reconciliation.md` discovery behavior; `configuration.md` path mappings; `data-and-recovery.md` filesystem execution; `connectors.md` filesystem boundary; A-19, A-21 and A-23.
- Intended result and capability limits: read-only `ports.FilesystemReadPort` implementation with bounded direct-child pages, opaque continuation cursors, SHA-256 hashing, device/inode-plus-size/mtime identity on Unix, exact root-relative manifests, component-aware upstream mapping and three-valued capability evidence. Unix paths resolve each component through directory descriptors with `O_NOFOLLOW`; root targets, traversal, symlinks and special files are rejected. Non-Unix fallback performs static no-symlink checks and reports no-follow capability as unknown.
- Changes completed / remaining: completed observer construction from configured roots, `Enumerate`/`EnumeratePage`, `Stat`, `Hash`, `Capabilities`, mapping selection/translation, permission probes and focused tests including sorted stable-name snapshot cursors that traverse beyond the per-page maximum, unsupported-child evidence that does not abort sibling pages, configured capability authority, directory replacement cursors and a Unix symlink-swap race. Filesystem mutations, recursive discovery grouping and storage persistence remain downstream lanes.
- Correction scope: the frozen `ports.Page` contract has no unsupported-child collection or reason field. This lane uses `domain.Coverage.ReasonCodes` as a compatibility bridge with the prefix `unsupported_child:` followed by compact JSON `{"relativePath":"...","reason":"..."}`; `ParseUnsupportedChildReasonCode` is the package helper for F-02. The coordinator should preserve this bridge or make the smallest explicit contract extension before discovery consumes it; unsupported children are never silently discarded or represented as valid manifests.
- Shared contract changes requested from coordinator: promote `golang.org/x/sys v0.47.0` from its current indirect root-module entry to a direct dependency because Unix descriptor-relative observation imports `golang.org/x/sys/unix`. No module files were changed in this lane.
- Changed paths and tested commit: `31572c442e81d3f3fa17e2835fb6d1232106b503` (initial implementation `d3fdb8e3e7755c061edfbbd0c05c535db559cbd3`; platform follow-up `fc0511e3535d70caeb50116872734bc0767df775`; pagination/unsupported-evidence/capability correction after round-one review).

## Verification

| Command or scenario | Commit / fixture version | Result / exit status | Evidence path |
| --- | --- | --- | --- |
| `GOWORK=off go test ./internal/filesystem/observe -count=1` | `31572c4` / temporary synthetic roots | Passed, exit 0 | command output in correction turn |
| `GOWORK=off go test -race ./internal/filesystem/observe -count=1` | `31572c4` / temporary synthetic roots | Passed, exit 0 | command output in correction turn |
| `GOWORK=off go vet ./internal/filesystem/observe` | `31572c4` | Passed, exit 0 | command output in correction turn |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go test -c -o /tmp/managerr-f01-correction-linux-amd64.test ./internal/filesystem/observe` | `31572c4` | Passed, exit 0 | command output in correction turn |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go test -c -o /tmp/managerr-f01-correction-linux-arm64.test ./internal/filesystem/observe` | `31572c4` | Passed, exit 0 | command output in correction turn |
| `GOWORK=off CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go test -c -o /tmp/managerr-f01-correction-windows-amd64.test ./internal/filesystem/observe` | `31572c4` | Passed, exit 0 | command output in correction turn |
| `GOWORK=off CGO_ENABLED=0 GOOS=aix GOARCH=ppc64 go test -c -o /tmp/managerr-f01-correction-aix-ppc64.test ./internal/filesystem/observe` | `31572c4` | Passed, exit 0 | command output in correction turn |
| `GOWORK=off go mod verify` | `31572c4` | Passed, exit 0; all modules verified | command output in correction turn |
| `TestObserveEnumerationCursorDoesNotDropProbedEntry` | `31572c4` / three synthetic files, page limit 1 | Passed; all three entries were returned once across multiple pages | `internal/filesystem/observe/observe_test.go` |
| `TestObserveEnumerationCursorTraversesBeyondPageMaximum` | `31572c4` / five synthetic files, maximum page size 2, page sizes 1 and 2 | Passed; complete unique result set traversed beyond the configured maximum | `internal/filesystem/observe/observe_test.go` |
| `TestObserveEnumerationSkipsUnsupportedChildren` | `31572c4` / regular files, symlink, FIFO and Unix socket | Passed; valid siblings returned once and each unsupported path carried a parsed reason | `internal/filesystem/observe/observe_unix_test.go` |
| `TestObserveCapabilitiesHonorConfiguredAuthority` | `31572c4` / writable, read-only, configured unsupported and unknown roots | Passed; configured states were retained and all read-only mutations remained unsupported | `internal/filesystem/observe/observe_test.go` |
| `TestObserveNeverFollowsDirectorySymlinkRace` | `31572c4` / synthetic in-root and outside files | Passed repeatedly and under `-race`; successful reads always matched in-root bytes | `internal/filesystem/observe/observe_unix_test.go` |
| traversal, root target, symlink, special file and path-prefix ambiguity cases | `31572c4` / temporary synthetic roots | Passed; no external writes are performed by observer | `internal/filesystem/observe/observe_test.go`, `observe_unix_test.go` |
| directory replacement after first bounded page | `31572c4` / temporary synthetic root | Passed; continuation returns `ErrChanged` instead of mixing manifests | `internal/filesystem/observe/observe_test.go` |
| `GOWORK=off go test ./...` | `31572c4` plus unrelated shared D-01 storage edits | Passed, exit 0; all packages passed while shared D-01 edits remained present | command output in correction turn |
| `git diff --check` | `31572c4` | Passed for lane changes; unrelated D-01 storage edits remained in the shared checkout | command output in correction turn |

No live media service, client, upstream write, credential, real inventory or destructive filesystem action was used. Observer APIs return relative paths and sanitized sentinel errors; host paths remain constructor-only values.

## Review and integration

- Reviewer identity/role and reviewed commit: round-one `/root/f01_reviewer` receipt `e8c58feb59418b7237759671a039150039e1a79b`; round-two review pending against `31572c442e81d3f3fa17e2835fb6d1232106b503`.
- Findings with severity, reproduction and contract reference: round-one P1s were cumulative cursor limit, unsupported-child page abort, and capability authority precedence; each has a focused correction and regression in `31572c4`.
- Fix commit and regression evidence: `31572c442e81d3f3fa17e2835fb6d1232106b503`; see the three named correction tests above and the structured reason-code bridge documented in this handoff.
- Final reviewer decision: pending.
- Integrated commit, recorded by coordinator: pending.

## Resume checkpoint

- Current state and outstanding uncertainty: correction implementation is committed and focused checks pass. Linux/Darwin descriptor-relative no-follow behavior is covered; non-Linux/Darwin fallback is portable but deliberately reports no-follow unknown. Direct `x/sys` module classification, shared D-01 integration state, Linux runtime execution and broader cross-system acceptance remain coordinator/reviewer work.
- Active process or agent ownership, sanitized: worker owns no active product edits after `31572c4`; shared checkout contains unrelated unstaged D-01 storage work that must be preserved.
- Next safe action: coordinator records exact correction commit in `state.json`, dispatches `/root/f01_reviewer` for round two against `31572c442e81d3f3fa17e2835fb6d1232106b503`, then promotes `x/sys` in root module during integration if review approves.
- Blocker and exact input/evidence needed: independent review must check root confinement, race behavior, page bounds/cursor integrity, capability honesty and module dependency hygiene.
- No conflicting writes or unknown files removed: confirmed; no reset, clean, rebase or deletion performed.
