# X-07 qBittorrent control adapter

## Assignment

- Task ID and title: X-07, implement qBittorrent control adapter.
- Owner/agent and independent reviewer: `/root/x05_implementer`; reviewer
  `/root/x05_reviewer`.
- Base commit: `f53cdd2` (`docs(execution): complete D-02 and X-05`).
- Branch/worktree or shared-checkout ownership: shared checkout on `main`;
  coordinator owns integration and `docs/execution/state.json`.
- Owned files and generated outputs: `internal/adapters/qbittorrent/control/`.
  No generated output or shared fixture path was changed.
- Dependencies verified at commits: X-05 write-safety evidence is present in
  the base checkpoint; its final product/review receipts are recorded by the
  coordinator. The nested qBittorrent module remains the read-only client at
  the base checkpoint.
- Required acceptance IDs and exact planned commands: A-28, A-29, A-30, A-31,
  A-33; focused control tests, race, vet, qBittorrent module matrix, module
  verification, Linux amd64/arm64 compile, generation, API, architecture,
  lint, planning and guardrail checks.

## Contract and work

- Linked specification sections and relevant invariants: X-07 in
  `docs/plans/implementation.md`; qBittorrent control and mapping rules in
  `docs/specs/spec-001-media-reconciliation/connectors.md` and
  `data-and-recovery.md`; A-28/A-29/A-30/A-31/A-33 in
  `docs/verification/acceptance.md`; protocol-model safety rules in
  `docs/research/write-capabilities.md`.
- Intended result and capability limits: expose `ports.DownloadControlPort`
  and `ports.CapabilityPort` through a root adapter. Observe remains
  read-only. Writes require an explicit supported capability with nonempty
  version and evidence; the default is unknown and blocks dispatch.
- Changes completed / remaining:
  - Added an injected, normalized `Upstream` seam. It accepts qBittorrent
    client DTOs and owns no HTTP, authentication or generated-code transport.
  - Added exact torrent observation and configured path mapping, including
    duplicate/ambiguous/unmapped path rejection.
  - Added stop with explicit stopped-state read-back. `upspeed=0` and a
    successful response never establish stopped state.
  - Added detached, bounded read-back after every dispatched write. A lost or
    conflicting response returns `outcome_unknown` and is never retried
    blindly.
  - Added whole-torrent relocation and file/folder rename. Folder rename is
    refused when any observed payload lies outside the reviewed folder, and
    native path collisions are rejected before dispatch.
  - Added metadata-only removal. The adapter always calls
    `Delete(..., false)`, requires a stopped torrent, reconciles record
    absence, and has no re-add or resume path.
  - Added synthetic fixture tests for zero-speed seeding, lost responses,
    external resume, idempotency, mapping, scope, collision, and exact
    read-back behavior.
  - Runtime wiring remains blocked until the nested qBittorrent module gains
    generated and transport-backed mutation methods under its own client lane.
- Shared contract changes requested from coordinator: none.
- Changed paths and tested commit: product commits
  `7f8d0998b7ff3c081d055051a461a39cd224b1b7` and
  `a51e519c6d84a6db194350d0e4cb8bd4e6f66b30` (current product checkpoint).

## Verification

| Command or scenario | Commit / fixture version | Result / exit status | Evidence path |
| --- | --- | --- | --- |
| `GOWORK=off go test -mod=readonly ./internal/adapters/qbittorrent/control` | `a51e519`; synthetic injected upstream | Passed | `internal/adapters/qbittorrent/control/control_test.go` |
| `GOWORK=off go test -race -mod=readonly ./internal/adapters/qbittorrent/control` | `a51e519` | Passed | control package race run |
| `GOWORK=off go vet -mod=readonly ./internal/adapters/qbittorrent/control` | `a51e519` | Passed | control package vet run |
| `GOWORK=off go test -mod=readonly -count=1 ./...` in `clients/qbittorrent` | nested qBittorrent module at base | Passed | standalone client tests |
| `GOWORK=off go test -race -mod=readonly -count=1 ./...` in `clients/qbittorrent` | nested qBittorrent module at base | Passed | standalone client race tests |
| `GOWORK=off go vet -mod=readonly ./...` in `clients/qbittorrent` | nested qBittorrent module at base | Passed | standalone client vet |
| `GOWORK=off go mod verify` in root and `clients/qbittorrent` | `a51e519` | Passed; all modules verified | module verification |
| `GOOS=linux GOARCH=amd64 GOWORK=off go test -mod=readonly -c -o /tmp/mastarr-control-linux-amd64.test ./internal/adapters/qbittorrent/control` | `a51e519` | Passed | Linux amd64 compile |
| `GOOS=linux GOARCH=arm64 GOWORK=off go test -mod=readonly -c -o /tmp/mastarr-control-linux-arm64.test ./internal/adapters/qbittorrent/control` | `a51e519` | Passed | Linux arm64 compile |
| `./scripts/generate.sh --check` | `a51e519` | Passed | generated output unchanged |
| `./scripts/check-api.sh` | `a51e519` | Passed; Vacuum quality 100/100 | bundled OpenAPI and Vacuum |
| `./scripts/check-lint.sh --architecture-only` | `a51e519` | Passed | root/client architecture checks |
| `python3 scripts/check_planning.py` | `a51e519` | Passed; 43 tasks, 60 acceptance cases | planning links |
| `./scripts/check-guardrails.sh --fast` | `a51e519` | Passed | generation/API/architecture/targeted guardrails |
| `git diff --check` and staged diff check | `a51e519` | Passed | whitespace validation |
| `GOWORK=off go test -mod=readonly ./...` in root | concurrent D-03 untracked package present | Blocked: Go reports `updates to go.mod needed`; no root files were changed by this lane | coordinator must complete D-03 module dependencies |
| `./scripts/check-guardrails.sh --ci` and `./scripts/check-lint.sh` | concurrent D-03 untracked package present | Blocked at root module `go list`/checks for the same pending `go.mod` update | coordinator checkpoint |
| direct `GOWORK=off golangci-lint run ./internal/adapters/qbittorrent/control` | host binary | Blocked before analysis: binary built with Go 1.26 cannot load target Go 1.27.1 config; repository script uses pinned tools module | rerun through repository toolchain after dependency checkpoint |
| Native qBittorrent process, credentials, private coordinates, live files or live mutations | intentionally excluded | Not run; runtime capability stays blocked | injected synthetic upstream only |

Recorded scenarios include:

- `stalledUP` with `UpSpeed=0` is observed as seeding and requires stop.
- A stop error after state changed to `pausedUP` resolves as applied from
  read-back; stop is called once.
- A later `stalledUP` observation blocks removal and does not implicitly stop
  or delete.
- Removal requires an explicitly stopped state, records `false` for the
  `deleteFiles` argument, retains synthetic payload in the fake client, and
  treats an absent record after a lost response as applied.
- File and folder collision/scope checks reject before native dispatch;
  successful native changes require exact mapped path read-back.
- A write error after a changed file path remains `outcome_unknown` when
  read-back cannot prove the requested outcome, with no second write.

## Review and integration

- Reviewer identity/role and reviewed commit: pending independent review by
  `/root/x05_reviewer` of `a51e519c6d84a6db194350d0e4cb8bd4e6f66b30`.
- Findings with severity, reproduction and contract reference: pending.
- Fix commit and regression evidence: no reviewer findings yet; product
  follow-up `a51e519c6d84a6db194350d0e4cb8bd4e6f66b30` uses the reconciled
  upstream torrent identity for every dispatched write.
- Final reviewer decision: pending.
- Integrated commit, recorded by coordinator: pending; state remains
  coordinator-owned.

## Resume checkpoint

- Current state and outstanding uncertainty: control package is product-ready
  for injected normalized upstream behavior. All dispatched writes reconcile
  through read-only observation; uncertain effects stay unknown. G-01 remains
  open for Arr and is outside this lane.
- Active process or agent ownership, sanitized: shared checkout on `main`;
  `/root/x05_reviewer` owns independent review. Concurrent D-03 owns
  `internal/configuration/` and related files; those edits remain untouched.
- Next safe action: reviewer checks the product commits; coordinator extends
  `clients/qbittorrent` with versioned generated mutation routes and transport
  methods, then wires that implementation to this adapter after runtime
  capability evidence passes.
- Blocker and exact input/evidence needed: current
  `clients/qbittorrent` module exposes only read methods. Add and pin
  qBittorrent stop, setLocation, renameFile, renameFolder and delete routes
  through its owned OpenAPI/generation lane, plus synthetic httptest evidence
  for the target build. Do not hand-roll those routes in this root adapter.
- No conflicting writes or unknown files removed: true. `docs/execution/state.json`,
  scripts, modules, D-03 files and shared compatibility fixtures were not
  changed.
