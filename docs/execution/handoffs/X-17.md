# X-17 standalone Jellyfin client module

## Assignment

- Task ID and title: X-17, build standalone Jellyfin client module.
- Owner/agent and independent reviewer: `/root/x05_implementer`; independent
  reviewer `/root/x05_reviewer` (coordinator assignment pending).
- Base commit: `b8b1f42ec86116eb087ef977885e322de7ba9bc1`.
- Branch/worktree or shared-checkout ownership, repository-relative public notation:
  shared checkout on `main`; the coordinator owns execution state and
  integration.
- Owned files and generated outputs: `clients/jellyfin/`, including committed
  `clients/jellyfin/internal/generated/client.gen.go`, and this handoff at
  `docs/execution/handoffs/X-17.md`.
- Dependencies verified at commits: C-05
  `0b219e9d86776382532eced5251d363322d6dc37`; X-04
  `b5594b668185245304f70a0f46c1f77ae7b76d7b`.
- Required acceptance IDs and exact planned commands: A-08, A-09, A-45 and
  A-55; standalone module tests, race, vet, module verification, offline
  generation/reproducibility, Vacuum, lint, architecture, planning,
  guardrails and Linux amd64/arm64 CGO-free builds.

## Contract and work

- Linked specification sections and relevant invariants: X-17 in
  `docs/plans/implementation.md`; Jellyfin in
  `docs/specs/spec-001-media-reconciliation/connectors.md`; A-08, A-09, A-45
  and A-55 in `docs/verification/acceptance.md`. Refresh acceptance is kept
  separate from later item or library availability, and provider identity is
  instance-scoped evidence.
- Intended result and capability limits: one independent Go module at
  `clients/jellyfin` with a Mastarr-owned OpenAPI 3.1.1 contract for system
  information, libraries, items/provider IDs and explicit refresh acceptance.
  The client does not register titles, import media, search, mutate files or
  claim that a refresh made an item available. Item refresh remains a caller
  capability decision until a versioned fixture and adapter gate authorize it.
- Changes completed / remaining: the client owns its generated DTO/client
  boundary, token transport, deadline and bounded strict decoding, redirect
  refusal, sanitized typed errors, library fallback, item scope/provider
  normalization, offset-page coverage and explicit refresh result. It rejects
  duplicate/trailing/deep JSON, missing required identity, duplicate IDs or
  sources, oversized responses and invalid scopes. Remaining work is the
  coordinator-owned matrix/bootstrap and later root adapter migration.
- Shared contract changes requested from coordinator: add `clients/jellyfin`
  to the aggregate generation, lint, test, vet, module-verification,
  cross-build and CI matrices in C-06, then publish a consumable module
  version or use the repository's approved bootstrap procedure. X-09/X-21
  must retain the refresh capability/version gate and translate these client
  observations without exposing generated DTOs.
- Changed paths and tested commit: product commit
  `f9e0754853e1855af37e61ac96c8bc70a7e017d4` (`fix(jellyfin): harden scoped
  observations`), following initial product commit
  `a48d7b3a6892c36a52c83293f1973739448e8c8c`.

## Verification

All module checks used synthetic `httptest` fixtures. No live Jellyfin
endpoint, credential, private coordinate, inventory, media data or file
operation was used.

| Command or scenario | Commit / fixture version | Result / exit status | Evidence path |
| --- | --- | --- | --- |
| `GOWORK=off go test -mod=readonly -count=1 -timeout=60s ./...` from `clients/jellyfin` | `f9e0754`; synthetic client fixtures | Passed, exit 0 | `clients/jellyfin/client_test.go` |
| `GOWORK=off go test -mod=readonly -race -count=3 -timeout=180s ./...` from `clients/jellyfin` | `f9e0754`; repeated fixture runs | Passed, exit 0 | `clients/jellyfin/client_test.go` |
| `GOWORK=off go vet -mod=readonly ./...` from `clients/jellyfin` | `f9e0754` | Passed, exit 0 | `clients/jellyfin/*.go` |
| `GOWORK=off go mod verify` from `clients/jellyfin` | `f9e0754` | Passed: `all modules verified` | `clients/jellyfin/go.mod`, `clients/jellyfin/go.sum` |
| `GOWORK=off GOPROXY=off GOSUMDB=off go generate ./...` from `clients/jellyfin` | `f9e0754`; pinned tools module | Passed; generated output unchanged | `clients/jellyfin/generate.go`, `clients/jellyfin/internal/generated/client.gen.go` |
| `GOWORK=off GOPROXY=off GOSUMDB=off ./check-generation.sh` from `clients/jellyfin` | `f9e0754` | Passed: `Jellyfin generation checks passed` | `clients/jellyfin/check-generation.sh` |
| Pinned Vacuum: `GOWORK=off GOPROXY=off GOSUMDB=off go tool -modfile=../../tools/go.mod github.com/daveshanley/vacuum lint --no-update-check --remote=false --ruleset ../../api/vacuum.yaml --fail-severity=warn --no-banner --no-style openapi.yaml` | `f9e0754`; OpenAPI 3.1.1 contract | Passed, quality 100/100, zero warnings/errors, exit 0 | `clients/jellyfin/openapi.yaml` |
| Pinned golangci-lint: `GOWORK=off go tool -modfile=../../tools/go.mod github.com/golangci/golangci-lint/v2/cmd/golangci-lint run --config ../../.golangci.yml --timeout=5m ./...` from `clients/jellyfin` | `f9e0754` | Passed: `0 issues.`, exit 0 | `clients/jellyfin/*.go` |
| `GOOS=linux GOARCH=amd64 CGO_ENABLED=0 GOWORK=off go build -mod=readonly ./...` from `clients/jellyfin` | `f9e0754` | Passed, exit 0 | `clients/jellyfin` |
| `GOOS=linux GOARCH=arm64 CGO_ENABLED=0 GOWORK=off go build -mod=readonly ./...` from `clients/jellyfin` | `f9e0754` | Passed, exit 0 | `clients/jellyfin` |
| `./scripts/check-guardrails.sh --ci` from repository root | `f9e0754`; aggregate repository fixtures and matrices | Passed: API/Vacuum quality 100/100, architecture, lint, tests, vet, generation and module verification; exit 0 | `scripts/check-guardrails.sh` |
| Focused scenarios | `f9e0754`; synthetic `httptest` | Passed: token/system info; folder/user-view library fallback; provider and media-source evidence; foreign/missing item rejection; malformed/duplicate/trailing/deep/oversized responses; explicit refresh acceptance followed by separate unavailable/visible reads; unsupported item refresh; timeout/configuration and redirect handling | `clients/jellyfin/client_test.go` |

The aggregate guardrail script currently omits `clients/jellyfin`; the direct
module checks above are the evidence for this lane until C-06 updates it.

## Review and integration

- Reviewer identity/role and reviewed commit: `/root/x05_reviewer`, independent
  review of `f9e0754853e1855af37e61ac96c8bc70a7e017d4`, pending.
- Findings with severity, reproduction and contract reference: pending
  independent review.
- Fix commit and regression evidence: no review corrections pending; the
  hardening product commit is `f9e0754853e1855af37e61ac96c8bc70a7e017d4`.
- Final reviewer decision: pending.
- Integrated commit, recorded by coordinator: pending; `state.json` was not
  edited by this lane.

## Resume checkpoint

- Current state and outstanding uncertainty: product is complete and the
  working tree was clean before this handoff. The module has no pinned
  Jellyfin release in the compatibility matrix, so runtime enablement remains
  a version/fixture and deployment path-mapping decision. Root adapter refresh
  remains outside this lane.
- Active process or agent ownership, sanitized: no active process; reviewer
  and coordinator own the next review/state steps.
- Next safe action: commit this handoff separately, assign independent review
  against the exact product SHA, then record both SHAs and the review result in
  coordinator state before adapter migration.
- Blocker and exact input/evidence needed: C-06 matrix/bootstrap needs the
  module path and an approved published or bootstrap dependency; X-09/X-21
  need versioned native refresh evidence and root translation. No live
  credentials or coordinates are needed for those changes.
- No conflicting writes or unknown files removed: only `clients/jellyfin/`
  and this handoff are owned here; no state, root adapter, other client, UI,
  script or unrelated file was changed.
