# D-01 handoff

## Assignment

- Task ID and title: D-01, Create SQLite schema, migrations and sqlc repository.
- Owner/agent and independent reviewer: `/root/d01_implementer`; next `/root/d01_reviewer`.
- Base commit: `e1e3ff664f210e6dfe6565c649d02c9fdc003f16`.
- Branch/worktree or shared-checkout ownership, repository-relative public notation: shared checkout on `main`; product commit is isolated to the D-01 owned paths.
- Owned files and generated outputs: `internal/storage/`, `migrations/`, `sqlc.yaml`, and this handoff. Generated sqlc files live under `internal/storage/sqlc/`.
- Dependencies verified at commits: C-03 approved implementation `3d4b38637f83ff2c11ccaaad2e1ac5157783c491`, receipt `8c9bf9a6277e068f9ae6b859fab2c5f7aa4890a1`; C-02 tool pins are present in `tools/go.mod`.
- Required acceptance IDs and exact planned commands: A-32, A-36, A-43, A-45; `GOWORK=off go test ./...`, `GOWORK=off go test -race ./internal/...`, `GOWORK=off go vet ./...`, `./scripts/generate.sh --check`, `GOWORK=off go mod verify`, Linux amd64/arm64 CGO-free compile, and migration/constraint/lock tests.

## Contract and work

- Linked specification sections and relevant invariants: `docs/specs/spec-001-media-reconciliation/data-and-recovery.md` persistence, transaction, action queue, coverage and trash sections; `configuration.md` source ownership; `http-api.md` plan/action/coverage state values; `docs/architecture/workflow-patterns.md` polling queue and no duplicate outbox.
- Intended result and capability limits: SQLite remains the local journal while upstream systems and mounted files remain authoritative. Migrations use golang-migrate with embedded public SQL. sqlc generates typed access and `Store.WithTx` supplies short transaction boundaries. A Unix advisory lock rejects a second active owner and is released by the kernel on process exit; a Windows fallback uses an exclusive lock file. `action_runs` is the durable polling queue; no outbox is introduced before an external delivery exists.
- Changes completed / remaining: Added initial schema for source snapshots, connections, roots, mappings, encrypted envelopes, scans/coverage, discoveries/file observations, downloads/provenance/descriptors, media and per-instance external records/tracking, immutable plan revisions/manifests/decisions, workflows, action runs/attempts/effects, idempotency, trash/items/janitor and audit. Added v2 lookup indexes. Added foreign-key/JSON/path/enum/retention checks, immutable-record triggers, complete-coverage requirement for absence, connection-scoped tracking trigger, migration dirty-state refusal, commit/rollback helper, generated queries and focused tests. Full worker crash injection, upstream effects, filesystem reservation and backup/restore remain downstream tasks.
- Shared contract changes requested from coordinator: Root `go.mod` already declares migrate and modernc SQLite, but root `go.sum` lacks transitive checksums until coordinator refreshes module metadata after integrating this storage import. No root dependency files were included in the D-01 commit. The generated SQLC package exposes `sqlc.Queries`; downstream repositories should use `Store.Queries()` and `Store.WithTx`.
- Changed paths and tested commit: `293acffaf250cabdda2c9295f1f612724ef350e4` (`feat: add durable sqlite storage journal`).

## Verification

| Command or scenario | Commit / fixture version | Result / exit status | Evidence path |
| --- | --- | --- | --- |
| `GOWORK=off go test -mod=mod ./internal/storage/... ./migrations` | `293acff`, temporary `-mod=mod` checksum resolution | Passed, exit 0; fresh v2 migration, schema inventory, upgrade, dirty-state refusal, constraints, transaction semantics and lock tests | `internal/storage/store_test.go` |
| `GOWORK=off go test -mod=mod ./...` | `293acff`, synthetic SQLite and config fixtures | Passed, exit 0 | command output from D-01 implementation turn |
| `GOWORK=off go test -mod=mod -race ./internal/storage/...` | `293acff` | Passed, exit 0 | command output from D-01 implementation turn |
| `GOWORK=off go vet -mod=mod ./internal/storage/...` | `293acff` | Passed, exit 0 | command output from D-01 implementation turn |
| `./scripts/generate.sh --check` | pinned sqlc `v1.31.1`, envdoc `v1.11.0`, oapi generator `v2.8.0` | Passed, exit 0; generated SQLC output matches checked files | `sqlc.yaml`, `internal/storage/sqlc/` |
| `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 GOWORK=off go test -mod=mod -run '^$' -exec=true ./internal/storage/...` | `293acff` | Passed, exit 0 | command output from D-01 implementation turn |
| `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 GOWORK=off go test -mod=mod -run '^$' -exec=true ./internal/storage/...` | `293acff` | Passed, exit 0 | command output from D-01 implementation turn |
| `GOWORK=off go mod verify` | `293acff` | Passed, exit 0 | command output from D-01 implementation turn |
| `(cd tools && GOWORK=off go test ./... && GOWORK=off go vet ./... && GOWORK=off go mod tidy -diff)` | C-02 pinned tools | Passed, exit 0 | command output from D-01 implementation turn |
| A-32 focused contribution | synthetic transaction callback | Passed: `WithTx` commits all callback writes or rolls them back; SQLC supports the coordinator's approval/queue/audit transaction boundary. Full crash fault injection remains W-02/W-04/V-01. | `internal/storage/store_test.go:157` |
| A-36 focused contribution | second local store on same SQLite path | Passed: advisory lock rejects second owner until first closes; generated `ClaimActionRun` uses version/lease CAS. Cross-action path/torrent reservations remain W-02. | `internal/storage/store_test.go:134`, `internal/storage/query.sql:467` |
| A-43 focused contribution | fresh v2, v1-to-v2 index upgrade, dirty migration | Passed: schema migrates, v2 upgrade applies, dirty schema blocks startup, second owner is rejected. Interrupted process simulation and isolated backup restore remain D-04/V-01. | `internal/storage/store_test.go:24`, `internal/storage/store_test.go:66`, `internal/storage/store_test.go:109`, `internal/storage/store_test.go:134` |
| A-45 focused contribution | pinned tools and generated SQLC | Passed for SQLC/storage slice; full API/client/envdoc/templ regeneration remains coordinator integration scope. | `sqlc.yaml`, `scripts/generate.sh` |
| Public secret/path scan and `git diff --check` | synthetic `.invalid` endpoints and fixture paths | Passed, no credentials, tracker coordinates, private paths or whitespace errors in D-01 paths | D-01 implementation turn |

Skipped/blocked checks: standard `GOWORK=off go test ./...` and `go test -race` without `-mod=mod` require coordinator-owned root `go.sum` transitive entries for the already-declared modernc SQLite dependency; coordinator must refresh and rerun them. No live media service, upstream write, filesystem mutation, backup restore or release/deployment operation was run.

## Review and integration

- Reviewer identity/role and reviewed commit: pending independent reviewer `/root/d01_reviewer` against `293acffaf250cabdda2c9295f1f612724ef350e4`.
- Findings with severity, reproduction and contract reference: none from implementation self-check. Schema enum values intentionally follow frozen API/domain values: plan `preparing/ready/invalid/expired`, workflow/action/attempt states from C-03, and tracking `present/absent/unknown`; approval remains an immutable `approve/reject` decision while the plan remains `ready`.
- Fix commit and regression evidence: no review fixes yet; focused tests, race tests, vet, generation, module verification and Linux cross-compiles passed at the implementation commit above.
- Final reviewer decision: pending.
- Integrated commit, recorded by coordinator: pending; coordinator owns `docs/execution/state.json`.

## Resume checkpoint

- Current state and outstanding uncertainty: D-01 product is committed at `293acffaf250cabdda2c9295f1f612724ef350e4`; parent state is advancing independently. Root dependency checksum refresh and full standard gates remain coordinator work. Migration uses a separate SQL handle because golang-migrate closes its driver on runner close; this is covered by the fresh-open tests.
- Active process or agent ownership, sanitized: no D-01 worker is modifying owned product paths after this commit; unrelated `ui/reviewu00/` workspace edits remain untouched.
- Next safe action: `/root/d01_reviewer` reviews exact commit, especially enum/coverage semantics, append-only triggers, foreign keys, migration dirty behavior, SQLite pragmas and process-lock release. Coordinator then refreshes root module sums, reruns standard gates and integrates review evidence.
- Blocker and exact input/evidence needed: no product blocker. Need independent reviewer receipt and coordinator-owned root `go.sum` refresh before claiming A-43/A-45 repository-wide completion.
- No conflicting writes or unknown files removed: confirmed; only D-01 owned paths and this handoff were committed.

## Correction round 2

This section supersedes the historical implementation-round verification note
above: coordinator-owned root module checks were rerun after dependency
checksum refresh and passed for this correction.

- Independent review input: round-one changes-requested receipt
  `d35a719465b3f870bb1c2e70a16d0eb18051aa40`.
- Correction base: `3c1c3ced825d9f3d0035347ee48cb14457776724` (the shared checkout
  had advanced with unrelated F-01 review work; no F-01 paths are in this
  correction).
- Correction implementation: `d713fe432db106503d49870def691af5d4dbc911`,
  `fix: close D-01 storage contract gaps`.
- Owned paths committed: `internal/storage/`, `migrations/000003_storage_contract.up.sql`,
  `migrations/000003_storage_contract.down.sql`, `sqlc.yaml`, and generated
  SQLC files under `internal/storage/sqlc/`. The historical
  `migrations/000001_initial.up.sql` blob is unchanged from the original
  implementation commit; all correction schema changes are in ordered
  migration 000003.

The correction closes the seven review findings. Absent tracking evidence now
requires a non-null connection, media identity, root, positive freshness bound,
and the same connection/root/media identity in a complete, completed, fresh
coverage snapshot. Unknown observations may retain a connection without an
external record. Tracking identity and source-scope triggers reject mismatches.
Action runs and attempts have generated startup/expired-lease recovery,
cancellation and deadline terminalization queries; recovered dispatch attempts
remain uncertain and reconciling work stays selectable for read-only evidence.
Janitor records use versioned claims and triggers that atomically claim the
shared trash entry, enforce expiry for purge, exclude the opposite operation,
and retain operation state through recovery. Download persistence follows the
frozen `complete` API value, translating legacy `completed` and `paused` only
during migration. Composite foreign keys bind file observations to one
discovery scope, effects to one action run/attempt, and trash items to one
entry/root pair. Database and default lock paths are canonicalized and
revalidated so a database symlink alias cannot acquire a second owner. Source
snapshot/resource source mismatches fail closed on both new writes and upgrade
of historical rows.

Correction-specific regressions are in `internal/storage/store_test.go`:
v1-to-v3 and v2-to-v3 upgrades, legacy state translation, absent-coverage
scope/freshness negatives, source insert/update negatives, composite-scope
negatives, restart/expired action and janitor recovery, cancellation/deadline
terminalization, purge/restore claim ordering and expiry, in-memory support,
and database-symlink lock alias/release behavior. The down migration was also
exercised against a populated v3 fixture and restored the v2 shape, indexes and
legacy download translation.

The correction round passed these exact checks (all exit 0):

| Command or scenario | Result |
| --- | --- |
| `GOWORK=off go test -count=1 ./...` | Passed root packages, including storage, filesystem observers and migrations. |
| `GOWORK=off go test -count=1 -race ./internal/storage/...` | Passed. |
| `GOWORK=off go vet ./...` | Passed. |
| `./scripts/generate.sh --check` | Passed; SQLC output is reproducible. |
| `GOWORK=off go mod verify` | Passed; all modules verified. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `(cd tools && GOWORK=off go test ./... && GOWORK=off go vet ./... && GOWORK=off go mod tidy -diff)` | Passed. |
| `GOWORK=off go test -count=1 ./internal/storage/... ./migrations` | Passed focused migration/storage checks. |
| `git diff --check` | Passed before the correction commit. |
| `sha256sum migrations/000001_initial.up.sql` compared with the original implementation blob | Passed; immutable 000001 content retained. |

Acceptance contribution for this round: A-32 now has durable recovery and
transactional terminalization support; A-36 has canonical single-owner locking
and version/lease claims (cross-action reservations remain W-02); A-43 has
v1/v2-to-v3 migration coverage, dirty refusal and symlink-alias locking; and
A-45 has clean pinned SQLC generation and tools checks. Full approval/queue
crash injection, filesystem/upstream effects, backup/restore and live-stack
acceptance remain downstream. No root module files, state.json, UI files or
unrelated shared edits were staged by this correction.

- Next reviewer: `/root/d01_reviewer`, against `d713fe432db106503d49870def691af5d4dbc911`.
- Handoff receipt: pending this documentation commit.

## Correction round 3

This section supersedes the historical round-two verification and records the
bounded fixes requested by independent receipt
`2656a9bb9de4389b5170e17c6a420d0884eeffd3`.

- Correction dispatch input: round-two review receipt above; shared checkout
  was at `1aca2d07ef87cf2d3f322289aab0cada0d362984` when this round started and
  advanced with unrelated F-01 commits before the product commit.
- Product implementation: `0dce2b9b882130d892fa0e02ffaaf0e2f3761da3`,
  `fix: preserve legacy storage state during upgrade`.
- Owned product paths: `internal/storage/`, `migrations/000003_storage_contract.up.sql`,
  `migrations/000004_storage_compatibility.{up,down}.sql`, `sqlc.yaml`, and
  generated SQLC files under `internal/storage/sqlc/`. No root module files,
  `state.json`, UI files or F-01 paths were staged.
- Migration identity: `migrations/000001_initial.up.sql` remains byte-identical
  to the original implementation blob
  `ce05d9deb8317af700f191f1ac227b4e2a922b5b`. Compatibility handling needed by
  a v1/v2 journal is in the ordered 000003 copy policy; 000004 also repairs
  databases that had already reached the v3 shape.

The v1/v2 copy now preserves unscoped legacy tracking rows as `unknown`, derives
connection scope only from a referenced external record, downgrades unsafe
legacy `absent`/unscoped `present` rows to `unknown`, and wraps the original
status/evidence in explicit legacy evidence fields. Scope preflights run before
rebuilding file observations, action effects or trash items; a final
`PRAGMA foreign_key_check` fails the migration closed. In-flight legacy janitor
and trash rows are backfilled as one active operation with versioned claims;
dual, inverse-state and terminal conflicts abort before recovery records are
created. Unmatched active trash entries receive a deterministic reconciling
recovery record.

Persistent lock overrides must resolve to the canonical `<database>.lock`; only
in-memory stores retain an arbitrary test lock override. A new SQLC
`ClaimApprovedEarlyPurge` operation atomically binds a purge claim to the
immutable ready plan revision/digest, approving review decision, action run and
exact trash-entry version. Generic purge remains expiry-gated, while the manual
claim leaves `expires_at` unchanged. Approval metadata is attached only by that
claim, is immutable afterwards, and restore/purge entry claims remain mutually
exclusive.

Round-three regressions in `internal/storage/compatibility_round3_test.go` cover
populated v1/v2 tracking and janitor upgrade/reclaim, v1/v2/v3 file/effect/trash
scope mismatch refusal, ambiguous janitor operation refusal, noncanonical lock
overrides, early unapproved rejection, stale digest/version rejection, exact
approved acceptance and approval immutability. Existing tests were updated for
nullable legacy tracking connection scope and migration version 4.

The following checks passed after the product changes (all exit 0):

| Command or scenario | Result |
| --- | --- |
| `GOWORK=off go test -count=1 ./...` | Passed root packages, storage compatibility fixtures, filesystem observers and migrations. |
| `GOWORK=off go test -count=1 -race ./internal/storage/...` | Passed. |
| `GOWORK=off go vet ./...` | Passed. |
| `./scripts/generate.sh --check` | Passed; SQLC output is reproducible from the ordered schema. |
| `GOWORK=off go mod verify` | Passed. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `(cd tools && GOWORK=off go test ./... && GOWORK=off go vet ./... && GOWORK=off go mod tidy -diff)` | Passed. |
| `GOWORK=off go test -count=1 ./internal/storage/... ./migrations` | Passed focused storage/migration checks. |
| `git diff --check` and immutable 000001 SHA comparison | Passed; no whitespace errors and the historical up migration blob is unchanged. |

No live media service, credential, private inventory, upstream write,
filesystem mutation, release or deployment operation was used. Full worker
fault injection, external effects, backup/restore and live-stack acceptance
remain downstream. A temporary v4-to-v3 migration-down probe confirmed a clean
single step; no temporary probe file is part of the commit.

Acceptance contribution: A-32 gains durable legacy janitor/action recovery and
transactional terminalization support; A-36 gains canonical single-owner
locking, versioned claims and approved early-purge binding; A-43 gains
populated v1/v2/v3 migration refusal/translation coverage, dirty refusal and
Linux builds; A-45 gains clean pinned SQLC generation and tools checks. Worker
crash injection, filesystem/upstream effects and backup/restore remain later
lanes.

- Next reviewer: `/root/d01_reviewer`, against `0dce2b9b882130d892fa0e02ffaaf0e2f3761da3`.
- Handoff receipt: pending this documentation commit.

## Correction round 4

This section records the bounded fixes requested by independent receipt
`6c690d7c76c2979fa467edda8ac0a18663df45be`. The historical up migrations
`000001`, `000003` and `000004` were preserved byte-for-byte; the compatibility
changes are in ordered migration `000005_storage_safety`.

- Correction base observed in the shared checkout: `bc75aacbfda920272618d8e76118b4bc5f10df23`.
- Product implementation: `1a469f858b82b0f78b2d6d48c5e50999cdfdf00f`,
  `fix: bind storage upgrades to approved purge scope`.
- Owned product paths committed: `internal/storage/`,
  `migrations/000005_storage_safety.{up,down}.sql`, `sqlc.yaml`, and generated
  SQLC files under `internal/storage/sqlc/`. The correction tests are in
  `internal/storage/compatibility_round3_test.go`,
  `internal/storage/compatibility_round4_test.go` and
  `internal/storage/store_test.go`.
- Unrelated `internal/adapters/` work was untracked and was not staged.

The v5 migration keeps active tracking connection-scoped with a non-null
connection ID. It compares legacy observation connection and media identity to
the referenced external record, downgrades evidence whose scope cannot be
proved, and stores the original row in an append-only quarantine table excluded
from active tracking queries. Absent rows are also checked against the same
connection, media identity, root and fresh complete coverage snapshot before
they can remain active. A new unscoped unknown row is rejected by the active
schema.

The janitor upgrade preflights dual active or state-mismatched operations,
allows one matching running/reconciling operation alongside terminal history for
the opposite operation, backfills entry operation and worker lease fields, and
creates a deterministic reconciling recovery record for an orphaned in-flight
entry. Recovery ID and same-operation history collisions fail closed. The
existing purge/restore claim triggers remain mutually exclusive and retain
recoverable state after a failed operation.

Manual early purge now has an immutable relational target for exactly one
`fs.delete` plan revision, trash entry version and non-empty manifest. The
target must equal both the plan revision and trash manifest. Its atomic claim
validates the approving decision, ready/unexpired plan, exact action run and
version, worker/lease, cancellation/deadline boundary and entry state before
claiming the action run and janitor entry in the same SQLite statement. Generic
expiry purge remains separate. The v5 down migration restores the v4 active
tracking/janitor shape and removes the early-purge target table.

The following checks passed after the correction (all exit 0):

| Command or scenario | Result |
| --- | --- |
| `GOWORK=off go test -count=1 ./...` | Passed root packages, storage compatibility fixtures, filesystem observers and migrations. |
| `GOWORK=off go test -count=1 -race ./internal/storage/...` | Passed. |
| `GOWORK=off go vet ./...` | Passed. |
| `./scripts/generate.sh --check` | Passed; pinned SQLC, API/client and envdoc generation is clean. |
| `GOWORK=off go mod verify` | Passed. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `(cd tools && GOWORK=off go test ./... && GOWORK=off go vet ./... && GOWORK=off go mod tidy -diff)` | Passed. |
| `GOWORK=off go test -count=1 ./internal/storage/... ./migrations` | Passed focused migration/storage checks. |
| `TestRound4TrackingUpgradeQuarantinesFalseAndUnscopedRows` | Passed: mismatched, missing and unscoped legacy rows are quarantined; active unknown without connection is rejected; quarantine is immutable; final foreign-key check is clean. |
| `TestRound4JanitorUpgradeAllowsMatchingActiveWithTerminalHistory` | Passed for restore-after-failed-purge and purge-after-failed-restore; startup recovery and reclaim preserve the matching operation. |
| `TestRound4EarlyPurgeRejectsUnboundOrUnsafeClaims` | Passed: unrelated scope, unsupported kind, empty/mismatched manifest, waiting/needs-review state, cancellation, deadline and worker/lease mismatch are rejected; exact target claim remains supported. |
| `TestRound4MigrationDownRestoresV4Shape` | Passed on a populated v5 fixture; down reaches clean schema version 4, removes v5 target/action-run fields and preserves foreign-key integrity. |
| `sha256sum migrations/000001_initial.up.sql migrations/000003_storage_contract.up.sql migrations/000004_storage_compatibility.up.sql` | Passed immutable hashes: `1b5bd13d2184f5109b5483026783466ad1f5551c377fef174500496f34aacead`, `502c484fb39e822f047ea1ad05697b13ce30ed1d1bb8835d73183bbef7f80393`, `3c77b61d8f216f0ae985f9e244ff37cde5b6c6e41a2d6c73bf093f182edc6b2c`. |
| `git diff --check` and public secret/path scan | Passed; no credentials, private coordinates or live media data. |

Acceptance contribution for this round: A-32 gains a durable, cancellable,
deadline-aware approved purge dispatch boundary; A-36 gains exact janitor
operation/entry CAS binding and canonical single-owner recovery coverage; A-43
gains conservative populated v4-to-v5 tracking/janitor upgrades plus a tested
down path; and A-45 gains reproducible generated SQLC output. Full worker fault
injection, upstream/filesystem effects, backup/restore and repository-wide
release gates remain downstream. No live media service, upstream write,
filesystem mutation, credential, release or deployment operation was used.

- Next reviewer: `/root/d01_reviewer`, against `1a469f858b82b0f78b2d6d48c5e50999cdfdf00f`.
- Handoff receipt: pending this documentation commit.

## Correction round 5

This section records the bounded fixes requested by independent receipt
`ebae3157302c046b8f5497492064400c28333268` against the round-four product
`1a469f858b82b0f78b2d6d48c5e50999cdfdf00f`. The shared checkout had advanced
to `563afd8ca729977968223296fd2e0d5bb59e6e5d` with coordinator and unrelated
X-01 documentation changes; those paths were preserved and excluded from the
product commit.

- Product implementation: `c688f2cc5fcd59515943fb6e5c4baca361dca3b6`,
  `fix: close D-01 approval and downgrade safety gaps`.
- Owned product paths committed: `internal/storage/`,
  `migrations/000005_storage_safety.{up,down}.sql`,
  `migrations/000006_approval_safety.{up,down}.sql`, `sqlc.yaml`, and the
  focused storage tests. No generated SQLC source changed because the round
  adds trigger and migration behavior only; generation remains reproducible.
- Frozen migration blobs remain byte-identical for `000001`, `000003` and
  `000004`. The previously committed `000005` pair carries the required
  rollback collision guards and direct-down neutralization; new approval and
  preview-window rules are ordered in `000006`.

The approval target is now created and validated before an immutable approve
decision. Its plan revision, digest, nonempty manifest, trash entry, entry
version and creation timestamp are checked together; target inserts after an
approve and all target mutations are rejected. A standalone live `fs.delete`
plan with no matching trash manifest remains approvable; a delete matching
trash is treated as early purge and must use the exact target relation.

Legacy approval-bearing janitor records are moved to `held`, associated action
runs to `needs_review`, leases are cleared, prior state/outcome is retained in
the outcome journal, and generic expiry claims are rejected. Both the direct
v5-to-v4 down path and the v6-to-v4 path neutralize before removing the target
or action-run-version columns; the v4 guard remains in place and re-upgrade
does not invent a CAS version. Quarantine rows retain immutable update/delete
guards across rollback, exact duplicate legacy evidence is idempotent, and
changed same-ID evidence aborts before either generation is dropped. The
preflight compares identity, scope, status, evidence, coverage, timestamps and
the freshness bound. Existing janitor recovery still permits one matching
active operation alongside terminal history for the opposite operation.

Accepted approvals validate revision, target, decision and action creation
timestamps against preview expiry. Dispatch no longer compares current time to
preview expiry; it continues only while the action deadline is live and no
cancellation is requested. The atomic claim still checks plan/revision/digest,
decision, target entry/version/manifest, action-run version, worker lease and
trash state in one SQLite statement.

The following checks passed after the product commit (all exit 0):

| Command or scenario | Result |
| --- | --- |
| `GOWORK=off go test -count=1 ./internal/storage/...` | Passed, including all prior compatibility tests and round-five scenarios. |
| `GOWORK=off go test -count=1 -race ./internal/storage/...` | Passed. |
| `GOWORK=off go vet ./...` | Passed. |
| `./scripts/generate.sh --check` | Passed; SQLC and other generated outputs are clean. |
| `GOWORK=off go mod verify` | Passed. |
| `GOWORK=off go test -count=1 ./internal/storage/... ./migrations` | Passed. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `CGO_ENABLED=0 GOOS=linux GOARCH=arm64 GOWORK=off go test -count=1 -run '^$' -exec=true ./internal/storage/...` | Passed. |
| `(cd tools && GOWORK=off go test ./... && GOWORK=off go vet ./... && GOWORK=off go mod tidy -diff)` | Passed. |
| `git diff --check` | Passed for the product diff. |
| Round-five populated approval target, direct-down, quarantine and expiry tests | Passed: `go test -run 'TestRound5' ./internal/storage`. |
| `sha256sum` migration history check | Passed in `TestRound4MigrationHistoryIsImmutable`; `000001` `1b5bd13d2184f5109b5483026783466ad1f5551c377fef174500496f34aacead`, `000003` `502c484fb39e822f047ea1ad05697b13ce30ed1d1bb8835d73183bbef7f80393`, `000004` `3c77b61d8f216f0ae985f9e244ff37cde5b6c6e41a2d6c73bf093f182edc6b2c`, `000005 up` `dc394314e4e13eff00976028cedb0110b222eb30a5afe8bbef79a0e6156a65bf`, `000005 down` `1ae04987891fd42f5b4b3976666b97a34144863c0826d3e6efa873f4ce1b3781`, `000006 up` `145c1726d20277a72402222204fbc581fafc62380429d20a116126a038b618a9`, `000006 down` `c172ff330989cb1eeb6d98af7923daaababe036b5d6d1d9e406da4622fccae28`. |

The repository-wide `GOWORK=off go test -count=1 ./...` gate was attempted at
this exact shared checkout. Storage and all other packages passed, while the
unrelated in-progress X-01 qBittorrent inventory files failed
`TestNonAdjacentOverlapAndMutableSnapshotStayPartial` and
`TestCursorAuthenticationAndLargestAcceptedRoundTrip`. Those files were
unstaged and untouched by this product commit; the coordinator should rerun
the repository gate after the X-01 correction settles.

Acceptance contribution for this round: A-32 gains approval-before-target
ordering, accepted-after-preview-expiry dispatch and safe cancellation/deadline
checks; A-36 gains legacy approval neutralization, exact target/action/trash
CAS and generic-claim exclusion; A-43 gains populated direct-down/up safety and
append-only quarantine collision handling; and A-45 gains ordered migration and
reproducible SQLC/tool verification. Full worker fault injection, external
filesystem/upstream effects, backup/restore and live-stack acceptance remain
downstream. No live media service, credential, private inventory, upstream
write, filesystem mutation, release or deployment operation was used.

- Next reviewer: `/root/d01_reviewer`, against `c688f2cc5fcd59515943fb6e5c4baca361dca3b6`.
- Handoff receipt: pending this documentation commit.

## Correction round 6

This section records the bounded fixes requested by independent receipt
`docs/execution/handoffs/D-01-review-round5.md` (reviewer product commit
`d6837ed31f09c26e797a6e5d2b24982c5b6cbb61`). The dispatch base was
`d2e0566f68d2e64199037dc320b40c4426ee4f1c`; the shared checkout also carried
later coordinator and unrelated adapter commits, which were preserved.

- Product implementation: `5829da918aecad00346075b9f1cb29612f196e0d`,
  `fix(storage): recover approved purge work safely`.
- Owned product paths: `internal/storage/`,
  `migrations/000007_storage_recovery_safety.{up,down}.sql`, `sqlc.yaml`, and
  generated SQLC files under `internal/storage/sqlc/`. No root module files,
  `state.json`, UI files or unrelated adapter paths were staged.
- Historical migration files `000001` through `000006` remain unchanged. The
  supported v5-to-current startup path applies the ordered 000006 and 000007
  corrections before the store is served; 000007 closes the populated-v5
  compatibility gap without rewriting migration history.

The v7 upgrade preflights every populated early-purge target against its
immutable approving decision. A target created after approval, with a digest
mismatch, or with timestamps that cannot be parsed is retained as evidence but
holds the target's purge janitor and trash entry and moves undispatched action
work to `needs_review`. The target remains immutable. Both the historical v6
claim trigger and the new query require `target.created_at <=
decision.created_at`, so an unsafe target cannot become executable after the
upgrade.

Valid approval-bound purge work can now recover after restart. Startup recovery
moves the action and janitor to `reconciling` and releases their old leases. A
dedicated SQLC CAS then reclaims only the exact approved plan revision/digest,
decision, action run, target, trash manifest/version and purge operation. It
leases the janitor and shared trash entry for read-only reconciliation while
leaving the action run reconciling and unleased; no external mutation is
dispatched by this recovery claim. It checks the worker lease, cancellation,
action deadline, due time, versions and chronology in the same CAS boundary.
Incomplete legacy approval bindings remain held and are rejected by both the
generic and dedicated claim paths. A successful reconciliation can close the
janitor, action and trash entry through the existing versioned update queries.

Round-six regressions in `internal/storage/compatibility_round6_test.go` cover
a populated v5 post-approval target, chronology neutralization and claim
rejection; a valid approval-bound claim followed by close/reopen, startup
recovery, lease reacquisition and safe resolution; and an incomplete legacy
approval that cannot use the recovery claim. Existing round-five tests retain
the cancellation, deadline, target-binding and accepted-after-preview-expiry
coverage.

The following checks passed after the product commit (all exit 0):

| Command or scenario | Result |
| --- | --- |
| `GOWORK=off go test -count=1 ./...` | Passed all root packages, including adapters, storage and migrations. |
| `GOWORK=off go test -count=1 ./internal/storage/... ./migrations` | Passed focused storage and migration checks. |
| `GOWORK=off go test -race -count=1 ./internal/storage/...` | Passed; storage and generated SQLC packages completed in 73.154 seconds. |
| `GOWORK=off go test -count=1 -run 'TestRound4MigrationHistoryIsImmutable|TestRound6|TestRound5' ./internal/storage` | Passed migration-history, prior approval-safety and all round-six regressions. |
| `GOWORK=off go vet ./...` | Passed. |
| `./scripts/generate.sh --check` | Passed; generated SQLC and other generated outputs are reproducible. |
| `cd tools && GOWORK=off go tool github.com/sqlc-dev/sqlc/cmd/sqlc generate -f ../sqlc.yaml` | Passed pinned SQLC generation. |
| `cd tools && GOWORK=off go test ./...`, `go vet ./...`, `go mod tidy -diff`, `go mod verify` | Passed tools tests, vet, tidy-diff and module verification. |
| `GOWORK=off go mod verify` and `ui/ GOWORK=off go mod verify` | Passed root and UI module verification. |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go test -c ...` | Passed compile-only Linux amd64 storage test binary build. |
| `GOWORK=off CGO_ENABLED=0 GOOS=linux GOARCH=arm64 go test -c ...` | Passed compile-only Linux arm64 storage test binary build. |
| `python3 scripts/check_planning.py` | Passed: 38 tasks, 60 acceptance cases and local links resolve. |
| `git diff --check` and `git diff --cached --check` | Passed. |

The compile-only Linux commands used `go test -c -o /tmp/...` because this
Darwin arm64 host cannot execute amd64 or arm64 Linux test binaries. No live
media service, credential, private inventory, upstream write, filesystem
mutation, release or deployment operation was used. Full worker fault
injection, external filesystem/upstream effects, backup/restore and live-stack
acceptance remain downstream.

Acceptance contribution for this round: A-32 gains populated upgrade
neutralization and restart-safe approved-purge reconciliation; A-36 gains a
lease-bearing read-only recovery CAS while incomplete approvals remain
blocked; A-43 gains chronology and recovery coverage across the v5-to-current
upgrade; and A-45 gains reproducible SQLC, module, vet and Linux compile
evidence. The worker still needs independent review of the exact migration,
claim predicates and recovery state transitions.

- Next reviewer: `/root/d01_reviewer`, against `5829da918aecad00346075b9f1cb29612f196e0d`.
- Handoff receipt: `docs/execution/handoffs/D-01-review-round6.md`, pending independent review.
