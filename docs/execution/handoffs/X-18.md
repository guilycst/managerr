# X-18 standalone Seerr client module

## Assignment

- Task ID and title: X-18, build standalone Seerr client module.
- Owner/agent and independent reviewer: assigned by coordinator at dispatch.
- Base commit: pending dispatch.
- Branch/worktree: shared checkout on `main`; coordinator owns execution state.
- Owned product path: `clients/seerr/`.
- Dependencies: C-05 generation/CI and X-04 Seerr observations are complete.
- Acceptance contributions: A-08, A-09, A-45 and A-54.

## Contract and work

Create a narrow Mastarr-owned OpenAPI compatibility document for paginated media
and request reads. Generate committed typed output with the pinned oapi-codegen
tool. Keep configured API-key or bearer authentication, deadlines, bounded
decoding, pagination termination, typed upstream errors and synthetic httptest
fixtures inside this module. There are no Seerr writes in v0.0.1. Do not assume
hidden discovery APIs. The module must not import Mastarr root packages, domain
types, ports, storage, workflow, adapters or another client module.

No product or verification result is claimed yet. The coordinator records exact
product and review SHAs after implementation.

## Resume checkpoint

- Current state: planned; no active owner or product commit.
- Next safe action: dispatch a single writer with an independent reviewer and an
  exact clean base SHA.
- Blockers: a published module version is required before root adapter migration.
