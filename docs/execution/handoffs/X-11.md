# X-11 handoff: standalone NZBGet read client

## Assignment

- Task ID and title: X-11, Build standalone NZBGet client module
- Owner/agent and independent reviewer: `/root/d01_implementer`; `/root/f01_reviewer`
- Base commit: `05b2d6e6a2f99252fdcf71b13af5a49acdb0b69f` (assigned dispatch base)
- Branch/worktree or shared-checkout ownership, repository-relative public notation: shared checkout on `main`; product changes were staged only from the X-11 owned paths
- Owned files and generated outputs: `clients/nzbget/` and `tools/internal/nzbgetgen/`; `clients/nzbget/generated.go` is generated from `clients/nzbget/openrpc.json`
- Dependencies verified at commits: C-03 `8c9bf9a6277e068f9ae6b859fab2c5f7aa4890a1`
- Required acceptance IDs and exact planned commands: A-05, A-06, A-09, A-45; module tests/race/vet/mod verify, deterministic OpenRPC generation, synthetic JSON-RPC `httptest`, root checks and cross-compiles

## Contract and work

- Linked specification sections and relevant invariants: X-11 in `docs/plans/implementation.md`; X-11 in `docs/execution/tasks.json`; NZBGet compatibility evidence in `docs/research/compatibility-matrix.md` and `docs/research/upstream-evidence.md`; the read-only, no-fabrication and no-secret invariants in `AGENTS.md`.
- Intended result and capability limits: a public, independent Go module at `github.com/guilycst/mastarr/clients/nzbget` for the four initial read calls only. It emits JSON-RPC 1.1 requests with positional parameters, Basic authentication, bounded responses and monotonic request IDs; handwritten code classifies sanitized failures and normalizes wire evidence.
- Changes completed / remaining: completed in product commit `ed0d4d274e6fae5b6f59c2d40ab60f81aaadb421`.
  - `openrpc.json` is an OpenRPC 1.2.6 Mastarr-owned compatibility document with exactly `version`, `listgroups`, `listfiles`, and `history`, each using `paramStructure: by-position`.
  - `tools/internal/nzbgetgen` validates the exact method set, positional mode and references, emits formatted typed request/result DTOs and wrappers deterministically, and is exercised by a double-generation comparison against the committed output.
  - The client preserves NZBGet's `version` envelope member, positional argument ordering (`[]`, `[0]`, `[0,0,NZBID]`, `[Hidden]`), response ID matching, HTTP Basic auth, caller/request deadlines, bounded body reads, same-origin redirects, and read-only method allowlisting. It does not call `rpc.discover`.
  - Wire DTOs retain `Hi`/`Lo` unsigned 32-bit halves, nullable/version-specific fields and deprecated `ID` aliases. Normalized observations combine complete halves into unsigned 64-bit values, preserve native status/path evidence, sanitize optional URL evidence, and fail closed on contradictory history `ID`/`NZBID` identities.
  - Synthetic fixtures and `httptest` regressions cover requests/auth, all four results, nullables, aliases, 64-bit values, malformed/oversized/HTTP/JSON-RPC failures, timeout/cancellation, read-only rejection and concurrent request IDs.
- Shared contract changes requested from coordinator: none. X-13 owns translation into the root NZBGet adapter; C-05 owns shared CI/generation discovery and must invoke this nested tools module from its own module boundary.
- Changed paths and tested commit: `ed0d4d274e6fae5b6f59c2d40ab60f81aaadb421`; only `clients/nzbget/` and `tools/internal/nzbgetgen/` were included.

## Verification

| Command or scenario | Commit / fixture version | Result / exit status | Evidence path |
| --- | --- | --- | --- |
| `(cd clients/nzbget && GOWORK=off go test -count=1 -timeout=60s ./...)` | `ed0d4d2`; four synthetic `testdata` results and `httptest` handlers | Passed, exit 0 | `clients/nzbget/client_test.go` |
| `(cd clients/nzbget && GOWORK=off go test -race -count=1 ./...)` | `ed0d4d2`; concurrent request-ID regression | Passed, exit 0 | `clients/nzbget/client_test.go` |
| `(cd clients/nzbget && GOWORK=off go vet ./...)` | `ed0d4d2` | Passed, exit 0 | `clients/nzbget/` |
| `(cd clients/nzbget && GOWORK=off go mod verify)` | standalone module has no external requirements | Passed: `all modules verified` | `clients/nzbget/go.mod` |
| `(cd clients/nzbget && GOWORK=off go generate ./...)` | `openrpc.json` -> `generated.go` | Passed, exit 0 | `clients/nzbget/generate.go` |
| `(cd tools && GOWORK=off go test ./internal/nzbgetgen)` | deterministic generator tests | Passed, exit 0 | `tools/internal/nzbgetgen/main_test.go` |
| `(cd tools && GOWORK=off go test ./...)` | existing tools compatibility tests plus generator | Passed, exit 0 | `tools/` |
| `(cd tools && GOWORK=off go run ./internal/nzbgetgen -check -input ../clients/nzbget/openrpc.json -output ../clients/nzbget/generated.go)` | committed generated output | Passed, exit 0 | `clients/nzbget/generated.go` |
| `./clients/nzbget/check-generation.sh` | temporary-directory generation comparison | Passed: `NZBGet generation checks passed` | `clients/nzbget/check-generation.sh` |
| `gofmt -d $(find clients/nzbget tools/internal/nzbgetgen -name '*.go' -print)` and `git diff --check` | product commit | Passed with no output | owned Go files |
| `GOWORK=off go test ./...` | root module with concurrent lanes | Passed, exit 0 | root packages |
| `GOWORK=off go test -race ./...` | root module with concurrent lanes | Passed, exit 0 | root packages |
| `GOWORK=off go vet ./...` | root module | Passed, exit 0 | root packages |
| `./scripts/generate.sh --check` | existing root/UI/tools generation | Passed, exit 0 | repository generation checks |
| `./scripts/check-lint.sh` | root lint and architecture | Passed: `0 issues` and `Go lint and architecture checks passed` | repository lint checks |
| `./scripts/check-api.sh` | frozen OpenAPI contract | Passed: `API checks passed` | repository API checks |
| `python3 scripts/check-architecture.py` | root import boundaries | Passed: `architecture import boundaries passed` | repository architecture check |
| `./scripts/check-guardrails.sh --fast` | formatting, root/UI/API guardrails | Passed: `guardrail checks passed (fast)` | repository guardrail check |
| `python3 scripts/check_planning.py` | execution planning ledger | Passed: `Planning valid: 43 tasks, 60 acceptance cases; local links resolve.` | planning check |
| `CGO_ENABLED=0 GOOS=linux GOARCH=amd64/arm64` and `GOOS=windows GOARCH=amd64 go test -c` in `clients/nzbget` | `ed0d4d2` | Passed, all three targets produced non-empty test binaries | cross-compile check |
| live NZBGet, credentials, mutating methods or real media | intentionally excluded | Not run by design; all tests use synthetic fixtures and disposable `httptest` servers | this handoff |

## Review and integration

- Reviewer identity/role and reviewed commit: `/root/f01_reviewer`, pending independent review of `ed0d4d274e6fae5b6f59c2d40ab60f81aaadb421`
- Findings with severity, reproduction and contract reference: none received at handoff time
- Fix commit and regression evidence: not applicable before independent review
- Final reviewer decision: pending
- Integrated commit, recorded by coordinator: pending; coordinator owns `state.json`

## Resume checkpoint

- Current state and outstanding uncertainty: X-11 product is committed and the separate handoff is ready for review. The module is read-only and intentionally reports optional newer fields as absent when the upstream server omits them. Runtime NZBGet product build compatibility remains an external evidence gate; the pinned API documentation covers version 13 and later, while later fields are optional.
- Active process or agent ownership, sanitized: no active client test processes; X-11 product/handoff ownership remains with `/root/d01_implementer` until coordinator records integration.
- Next safe action: independent review, then X-13 can translate normalized observations and typed errors into the root adapter after a public module version/release is available.
- Blocker and exact input/evidence needed: X-13 cannot claim root adapter migration from this lane. It needs the published version/commit of `clients/nzbget` (or coordinator-approved CI/module wiring) and must retain positional correlation/partial coverage semantics without adding a local `replace` or `go.work`.
- No conflicting writes or unknown files removed: no unrelated paths were staged, reset, rebased, cleaned or deleted.
